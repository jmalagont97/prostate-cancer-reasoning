# Logbook

This hidden logbook tracks approved advancements and next tasks chronologically.
Entries follow the strict format:
```markdown
## [YYYY-MM-DD] - [Advancement Title or Technical Milestone]
- **Current Project State:** [Brief description of the pipeline/model state]
- **Specific Advancements:** [Detail of the changes, code, architecture, or data processed today]
- **Next Tasks:** [Immediate technical next steps]
```

## [2026-08-18] - SHAP / Distance-Attribution Exhaustive Threshold Optimization (exp_19)
- **Current Project State:** Subtarea 1.3 completada. Se usaron los descriptores continuos de atribución local de características (valores SHAP del Explainer de Kernel y contribuciones de distancia KNN) derivados del modelo de decisión tabular congelado ($T_{21}$) para predecir los 10 pesos de relevancia clínica oficial (`target_code_weight_*`) y derivar la secuencia de evidencia (`target_reveal_sequence_json`).
- **Specific Advancements:**
  - `run_shap_relevance_experiment.py`: Extracción de attributions de las 10 variables oficializadas a partir del modelo tabular congelado de Subtarea 1.1 (`ConfidenceWeightedKNN`).
  - Motor de Optimización de Umbrales Ordinales (4 clases: `0=not_used`, `1=noted`, `2=important`, `3=decisive`): Búsqueda exhaustiva global en NumPy de tríos de corte $(\tau_1^*, \tau_2^*, \tau_3^*)$ en entrenamiento.
  - Evaluación de rejilla de 8 condiciones sobre 50 particiones MCCV (70/18) y auditoría final LOO (88 pliegues).
  - Configuración Ganadora: `method_shap_kernel_mode_exact_free_scale_max_normalized`.
  - Resultado Secuencia de Evidencia (`reveal_sequence`): **LOO Section $\text{F1}_{\text{macro}} = \mathbf{0.5775}$** (vs $0.1049$ de atriubución por distancia KNN pura).
  - Resultado Pesos de Relevancia: **LOO $\text{MOE}_{\text{abs}} = 0.3465$** acumulado entre los 10 pesos clínicos.
  - Artefactos guardados: `DESIGN.md`, `IMPLEMENTATION.md`, `run_shap_relevance_experiment.py`, `summary.json`, `evaluation_scorecard.csv`, `per_variable_metrics.csv`, `predictions_loo.csv`, `reports/summary.md`.
- **Next Tasks:** Consolidar pipeline integrado Task 1 (Subtareas 1.1, 1.2 y 1.3) y preparar entrega.

## [2026-08-17] - Corrección exp_10/exp_11: preprocesamiento numérico y negaciones
- **Current Project State:** exp_10 y exp_11 corregidos y re-ejecutados. Preprocesamiento: eliminación de tokens numéricos (dígitos + números escritos) + stopwords de negación protegidos (`no`, `not`, `without`, `never`, `neither`, `nor`, `none`). Resultados anteriores eliminados y reemplazados.
- **Specific Advancements:**
  - Preprocesamiento corregido: `re.sub(r"[^a-z0-9\s-]", " ", text)` (conserva guiones); eliminación de tokens con dígitos (`re.search(r"\d", lemma)`); eliminación de números escritos (`token.like_num`); protección de negaciones (`token.text not in NEGATION_STOPWORDS`).
  - NEGATION_STOPWORDS = {"no", "not", "without", "never", "neither", "nor", "none"}. Términos clínicos como `denies`, `negative`, `absent`, `unremarkable` no son stopwords y sobreviven naturalmente.
  - exp_10 corregido: k=3, cosine, distance, confidence_weighted, max_features=2000. MCCV F1_macro=0.646, F1_yes=0.768, specificity=0.451. LOO F1_macro=0.661, F1_yes=0.765, specificity=0.500. vs viejo: LOO F1_macro +0.052, specificity +0.147.
  - exp_11 corregido: ganador es **nosvd** (sin SVD). MCCV F1_macro=0.646 = exp_10. LOO F1_macro=0.661 = exp_10. SVD20: MCCV 0.629, inferior a nosvd. SVD ya no mejora con preprocesamiento limpio.
  - El preprocesamiento limpio reduce vocabulario ruidoso y mejora la especificidad (0.353→0.500) manteniendo F1_yes (0.764→0.765). Promedio de palabras: 198→139.
  - exp_11 resultado: **SVD innecesario** con preprocesamiento limpio. Ganador idéntico a exp_10.
- **Next Tasks:** Fusión tabular+text (exp_12) usando el preprocesamiento corregido de exp_10 como representación textual.

## [2026-08-17] - Re-ejecución exp_4–exp_9: fuzzy v2 + selector F1→Brier (v3)
- **Current Project State:** Los 6 experimentos (exp_4–exp_9) fueron re-ejecutados con fuzzy v2 (probability-smoothing) y selector lexicográfico F1_macro → brier_score → F1_yes → balanced_accuracy → MCC. Resultados antiguos archivados como `results_v2_old_selector/`. Selector F1→Brier produce `confidence_weighted` como ganador en todos los experimentos donde fuzzy empata en F1_macro con standard.
- **Specific Advancements:**
  - Correcciones comunes aplicadas a los 6 runners: (1) fuzzy v2 `q = 0.5 + c_nn * (y_nn - 0.5)`, (2) `brier_score` = `mean((p-y)^2)` (lower=better), (3) ECE fix (p=1.0 incluido en último bin), (4) `select_best()` lexicográfico F1→Brier, (5) cross-condition tiebreak por brier_score, (6) top-5 display con brier_score, (7) `validate_fuzzy_formula()` (9 tests).
  - exp_4: Winner = `confidence_weighted` (antes standard). MCCV F1_macro=0.645, brier_score=0.274; LOO F1_macro=0.651, F1_yes=0.734, brier_score=0.275.
  - exp_5: Winner = `tau_0.60 confidence_weighted` (antes standard). MCCV F1_macro=0.667, brier_score=0.260; LOO F1_macro=0.689, F1_yes=0.759, brier_score=0.256. **Mejor resultado de la campaña.**
  - exp_6: Winner = `euclidean distance confidence_weighted` (antes euclidean uniform standard). MCCV F1_macro=0.550, brier_score=0.356; LOO F1_macro=0.573, F1_yes=0.685.
  - exp_7: Winner = `cosine uniform confidence_weighted`. MCCV F1_macro=0.514, brier_score=0.370; LOO F1_macro=0.511, F1_yes=0.661. Estandarización dañina.
  - exp_8: Winner = `no_prune euclidean distance confidence_weighted` (=exp_6). Pruning dañino para MRI. MCCV F1_macro=0.550, brier_score=0.356; LOO F1_macro=0.573.
  - exp_9: Winner = `pca_1 euclidean distance confidence_weighted`. MCCV F1_macro=0.579, brier_score=0.329; LOO F1_macro=0.607, F1_yes=0.692. pca_1 maximiza F1_macro; pca_23 punto robusto.
  - Discrepancia de grid exp_5/exp_8 resuelta: código usa `[0.30, 0.60, 0.80, 0.90]`, DESIGN.md corrregido.
  - Todos los docs (DESIGN.md, IMPLEMENTATION.md) actualizados con revision v3.
- **Next Tasks:** Generar `reports/summary.md` para cada experimento; análisis comparativo exp_4–exp_9; considerar fusión tabular+MRI (exp_10) con pca_23 como representación MRI.

## [2026-08-16] - KNN + PCA Dimensionality Reduction on MRI Embedding — resultados (exp_9)
- **Current Project State:** exp_9 completado y reporte escrito. PCA sobre embedding MRI de 1024 dims con grid de n_components∈{1, 23, 46, 69} + no_pca × 72 configs KNN × 50 MCCV splits = 18,000 evaluaciones; LOO para la mejor config por F1_macro. Total: 10.8 min.
- **Specific Advancements:**
  - Grid original de 8 valores geométricos [1,2,3,6,11,21,38,69] modificado a 4 valores lineales [1,23,46,69] durante implementación para cubrir el rango completo con menos condiciones.
  - pca_1 (1D, 80.13% var): MCCV F1_macro=0.5791±0.1139 (+0.029 vs no_pca), LOO F1_macro=0.6067 (+0.034), LOO F1_yes=0.6916. Mejor condición por F1_macro.
  - pca_23 (23D, 98.17% var): MCCV F1_macro=0.5623±0.0971 (+0.013 vs no_pca), F1_yes=0.6769 (superior a pca_1). Punto de operación robusto: reducción 97.75% dimensional, varianza residual 10.9× menor que pca_1, dentro de ±0.02 neutralidad.
  - pca_46 (46D, 99.56% var): MCCV F1_macro=0.5550±0.0859. pca_69 (69D, 100% var): idéntico a no_pca (0.5497), validación interna de implementación.
  - Relación no monótona entre varianza explicada y F1_macro: más componentes = más varianza pero peor rendimiento predictivo. PC1 captura dirección discriminativa dominante; componentes 2–23 codifican variabilidad intra-clase no útil para KNN.
  - pca_1 tiene la mayor variabilidad MCCV (std=0.1139, CV=0.197) con mínimo 0.257 — más frágil por splits. pca_23 es más estable (CV=0.173).
  - MRI sigue siendo inferior al tabular: exp_5 LOO F1_macro=0.689 vs pca_1 LOO 0.607.
  - Artefactos: DESIGN.md (Status=Complete), IMPLEMENTATION.md, summary.md con análisis pca_23 robusto, confusion matrices, explained variance plots, validation_report all_passed. Índice actualizado (Status=Complete, Verdict=⚠️ PARTIAL).
- **Next Tasks:** LOO para pca_23 (validar F1_yes superior); fusión tabular+MRI (exp_10) con pca_23 como representación MRI; análisis de asociación PC1 con label para determinar si la ventaja de pca_1 es discriminativa o artefacto de geometría.

## [2026-08-16] - KNN + Spearman Correlation Pruning on MRI Embedding (exp_8)
- **Current Project State:** exp_8 implementado y lanzado en tmux session 0. Spearman correlation pruning sobre los 1024 componentes del embedding MRI (raw, sin escalar) con clustering jerárquico complete linkage y selección por medoid. Grid: tau∈{0.30, 0.60, 0.80, 0.90} + no_prune × 72 configuraciones KNN × 50 MCCV splits = 18,000 evaluaciones; LOO con intersección de dimensiones seleccionadas.
- **Specific Advancements:**
  - Pruning adaptado de exp_5 para embedding MRI puro numérico: Spearman matrix (1024×1024) calculada con `scipy.stats.spearmanr` en cada fold de entrenamiento (70 casos).
  - Sin variables esenciales — todos los 1024 componentes tratados uniformemente con regla de medoid.
  - Sin OHE, sin MinMaxScaler, sin indicadores de ausencia — embedding crudo directo, solo columnas seleccionadas pasan a KNN.
  - Condiciones: no_prune (replica exp_6 internamente), tau_0.30, tau_0.60, tau_0.80, tau_0.90. Grid de umbrales replicado del runner ejecutado de exp_5.
  - Selección global por F1_macro (local primario), guardrail F1_yes (oficial). LOO usa intersección de dimensiones de los 50 splits MCCV.
  - Artefactos: DESIGN.md, IMPLEMENTATION.md, runner script `run_mri_embedding_pruning_experiment.py`, confusion matrices.
- **Next Tasks:** Esperar resultados de exp_8; si pruning mejora MRI, considerar fusión tabular+MRI (exp_9); si no mejora, explorar PCA o directamente fusión.

## [2026-08-16] - KNN on Standardized MRI Embedding (exp_7)
- **Current Project State:** exp_7 completado. StandardScaler por componente ajustado dentro de cada fold (train only) sobre los 1024 componentes del embedding MRI. Mismo grid de 72 configuraciones que exp_6.
- **Specific Advancements:**
  - Preprocesamiento: `StandardScaler` fit-on-train-only en cada split MCCV (70 casos) y cada fold LOO (87 casos). No PCA, no pruning, no eliminación de columnas de varianza cero.
  - Mejor config: k=1, cosine, uniform, standard (vs euclidean en exp_6 raw).
  - MCCV: F1_macro=0.514±0.097, F1_yes=0.672±0.126, balanced_acc=0.525, MCC=0.052.
  - LOO: F1_macro=0.511, F1_yes=0.661, balanced_acc=0.514, MCC=0.029.
  - Comparación con exp_6: MCCV F1_macro -0.036, LOO F1_macro -0.062. **La estandarización dañó el rendimiento significativamente.**
  - Cosine recuperó la superioridad sobre euclidean después de la estandarización (opuesto a exp_6 raw), confirmando que la estandarización altera la geometría angular del embedding.
  - Artefactos: `experiments/exp_7/results/knn_n1_metriccosine_weightsuniform_variantstandard/` con metrics_mccv.json, metrics_loo.json, hyperparameters.json, oof_predictions_mccv.csv, oof_predictions_loo.csv, confusion_matrices.json, validation_report.json (all_passed=true, scaler_applied=true), git_commit.txt. Plus summary_selection.json, config_log.json. Figuras en `reports/figures/confusion_matrices.png/pdf`.
- **Next Tasks:** Fusionar modalidades (tabular + MRI) directamente sin estandarización MRI; explorar reducción dimensional del embedding MRI (PCA) como experimento alternativo; considerar que el embedding bruto es la modalidad MRI óptima para KNN.

## [2026-08-16] - KNN on Full MRI Embedding (exp_6)
- **Current Project State:** exp_6 ejecutado y verificado. KNN sobre los 1024 componentes del embedding MRI de `images.csv` sin PCA, sin correlation pruning, sin MinMaxScaler ni ninguna transformación por dimensión. Mismo grid de 72 configuraciones KNN que exp_4/exp_5. Preprocesamiento: solo validación de NaN/Inf y conversión a float64.
- **Specific Advancements:**
  - Embedding MRI nativo: 1024 dimensiones, rango [-28.85, 10.22], normas de fila [40.72, 72.70], cero NaN/Inf en la cohorte usable.
  - Grid KNN idéntico: k∈{1,3,5,7,9,11,15,21,31}, métrica∈{euclidean,cosine}, peso∈{uniform,distance}, variante∈{standard,confidence_weighted} = 72 configs × 50 splits = 3600 evaluaciones.
  - Mejor configuración: k=1, euclidean, uniform, standard → MCCV F1_macro=0.550±0.101, F1_yes=0.667±0.125, balanced_acc=0.554, MCC=0.118. LOO F1_macro=0.573, F1_yes=0.685, balanced_acc=0.572, MCC=0.148, Brier=0.398.
  - Comparación con tabular: exp_6 es significativamente inferior a exp_5 (MCCV F1_macro -0.117, LOO F1_macro -0.116) y a exp_4 (MCCV F1_macro -0.095, LOO F1_macro -0.078).
  - Análisis de config_log: con k=1, todas las variantes de peso y variante producen la misma F1_macro (0.550 euclidean, 0.507 cosine) porque la predicción binaria es idéntica con un solo vecino; solo difieren métricas basadas en probabilidad (PR-AUC, ROC-AUC, Brier, ECE).
  - Cosine es inferior a euclidean para embeddings MRI (F1_macro 0.507 vs 0.550), a diferencia del tabular donde cosine era superior.
  - Figuras de confusión generadas: `experiments/exp_6/reports/figures/confusion_matrices.png` y `.pdf` con paneles MCCV pooled (900 predicciones) y LOO (88 predicciones), absolutos y normalizados.
  - Artefactos: `experiments/exp_6/results/knn_n1_metriceuclidean_weightsuniform_variantstandard/` con metrics_mccv.json, metrics_loo.json, hyperparameters.json, oof_predictions_mccv.csv, oof_predictions_loo.csv, confusion_matrices.json, validation_report.json (all_passed=true), git_commit.txt. Plus summary_selection.json, config_log.json.
- **Next Tasks:** Fusionar modalidades (tabular + MRI) en exp_7 o exp_8 para determinar si MRI aporta información complementaria al tabular a pesar de ser inferior como modalidad única; explorar reducción dimensional del embedding MRI (PCA) como experimento separado.

## [2026-08-16] - KNN + Spearman Correlation Pruning (exp_5)
- **Current Project State:** exp_5 ejecutado y verificado. Extensión de exp_4 con correlation pruning como hiperparámetro: Spearman association matrix a nivel de variable original (max-abs para mixtas), clustering jerárquico complete linkage, representantes por regla de variables esenciales. Búsqueda de 5 condiciones (no_prune + tau∈{0.30, 0.60, 0.80, 0.90}) × 72 configuraciones KNN × 50 MCCV splits = 18,000 evaluaciones; LOO con intersección de variables seleccionadas por el umbral ganador.
- **Specific Advancements:**
  - Correlation pruning: Spearman homogéneo para todas las combinaciones de tipo. Categórica vs numérica: max(|Spearman|) sobre dummies (excluyendo sentinel "0"). Categórica vs categórica: max(|Spearman|) sobre pares de dummies. Matriz de asociación a nivel de variable original (no por dummy). Indicadores de ausencia separados del pruning primario.
  - Clustering: distancia D=1-A, linkage complete, corte en distance_cut=1-tau. Selección de representantes: variables esenciales (age, fh, cspca, pirads, vol, psa, comorbidity, psad, dre, bx) siempre protegidas; clusters sin esenciales usan medoid.
  - Pruning fold-local en MCCV (sin fuga); LOO usa intersección fija de las 50 selecciones MCCV por umbral.
  - Bug corregido durante ejecución: (1) fuga de información — matriz de asociación se computaba sobre todo el dataset en lugar del fold de entrenamiento; (2) umbral ignorado — `select_representatives()` usaba valores hard-coded [0.10, 0.15, 0.20] en vez del tau recibido.
  - Grid de umbrales final: tau∈{0.30, 0.60, 0.80, 0.90}. Resultados MCCV: tau_0.30→20 vars (intersección=11), tau_0.60→21 vars (intersección=21), tau_0.80→22 vars (intersección=22), tau_0.90→24 vars (intersección=22).
  - Mejor condición: tau=0.60, k=1, cosine, uniform, standard. Variables pruned (6): `cli_psap`, `path_hist_bx_gl_prim`, `path_hist_bx_gl_sec`, `path_hist_bx_gl_tert`, `path_hist_bx_isup`, `vit_bmi`. 21 variables retenidas incluyen las 10 esenciales.
  - MCCV F1_macro=0.667±0.099, F1_yes=0.738±0.095, balanced_acc=0.670, MCC=0.358. LOO F1_macro=0.689, F1_yes=0.759, balanced_acc=0.689, MCC=0.377, Brier=0.296.
  - Comparación con exp_4: F1_macro +0.038 (+5.8%), F1_yes +0.025 (+3.4%), balanced_acc +0.039 (+5.9%), MCC +0.076 (+25.2%), Brier -10.3%.
  - Artefactos: `experiments/exp_5/results/tau_0.60_knn_n1_metriccosine_weightsuniform_variantstandard/` con metrics_mccv.json, metrics_loo.json, hyperparameters.json, pruning_log.json, loo_intersection.json, oof_predictions_mccv.csv, oof_predictions_loo.csv, validation_report.json. Plus pruning_report.json, feature_frequency_*.csv, summary_selection.json.
- **Next Tasks:** Baselines con modelos discriminativos (logistic regression, random forest, gradient boosting) sobre las 21 variables de tau_0.60; comparar con exp_5; explorar feature selection alternativa.

## [2026-08-16] - KNN Baseline on All Tabular Variables (exp_4)
- **Current Project State:** exp_4 ejecutado. KNN sobre las 27 variables tabulares de `main_tabular.csv` (sin `case_id`, sin `path_hist_bx_gl_tert` excluido por >50% ausencia). Preprocesamiento leak-safe por fold: zero-fill + indicadores de ausencia, one-hot encoding, MinMax scaling. Búsqueda de 72 configuraciones (k×métrica×peso×variante) en 50 MCCV splits; evaluación LOO de la mejor configuración.
- **Specific Advancements:**
  - Preprocesamiento: sin imputación estadística; reemplazo determinista NaN→0 + indicador binario `<feature>__is_missing`. Variables categóricas: fillna("0") → str. Variables numéricas: fillna(0) → float64. One-hot (`handle_unknown='ignore'`) + MinMaxScaler fit-on-train-only.
  - Grid de búsqueda: k∈{1,3,5,7,9,11,15,21,31}, métrica∈{euclidean,cosine}, peso∈{uniform,distance}, variante∈{standard,confidence_weighted} = 72 configs × 50 splits = 3600 evaluaciones.
  - Variante fuzzy: `peso_final = peso_distancia × peso_confianza`, con `target_confiance` (clear=1.0, borderline=0.5, uncertain=0.25) aplicada solo a vecinos de entrenamiento.
  - Mejor configuración (selección por F1_macro): `knn_n1_metriccosine_weightsuniform_variantstandard` → MCCV F1_macro=0.645±0.104, F1_yes=0.726±0.095. LOO F1_macro=0.651, F1_yes=0.734, balanced_accuracy=0.650, MCC=0.301.
  - Artefactos: `experiments/exp_4/results/knn_n1_metriccosine_weightsuniform_variantstandard/` con metrics_mccv.json, metrics_loo.json, hyperparameters.json, config_log.json, oof_predictions_mccv.csv, oof_predictions_loo.csv, validation_report.json.
  - Criterio de selección local: F1_macro como primario (desviación documentada de `docs/EVALUATION.md` que fija `F1_yes`).
  - Nota: k=1 indica que el modelo tiende a memorizar; la estabilidad MCCV↔LOO sugiere que la estructura de datos es determinante. La specificity (0.56) es notablemente menor que la sensitivity (0.74), indicando más falsos positivos.
- **Next Tasks:** Baselines con modelos discriminativos (logistic regression, random forest, gradient boosting) sobre `main_tabular.csv`; comparar con exp_4; explorar feature selection sobre las 27 variables.

## [2026-08-16] - Corrección de exp_3: reorganización multimodal de entradas (3 vistas)
- **Current Project State:** exp_3 corregido. El contrato anterior de 7 vistas por grupo de `reveal_sequence` + `feature_manifest.csv` se sustituye por 3 vistas mínimas de entrada para el pipeline de tres modelos (decisión, confianza, relevancia). Los artefactos del esquema anterior fueron eliminados.
- **Specific Advancements:**
  - Nuevo script `experiments/exp_3/scripts/build_multimodal_views.py` (determinista) que genera en `data/chimera26/preprocessed/task1/`:
    - `main_tabular.csv` (195×28): `case_id` + 15 `cli_*` (incluye `cli_fh_binary` y `cli_comorbidity_count`), 8 `vit_*`, 4 `path_hist_*`; sin columnas `txt_*`.
    - `full_prompt_narrative.csv` (195×2): `case_id` + `txt_full_prompt_narrative`.
    - `images.csv` (195×1025): `case_id` + `mri_emb_0…1023`.
  - Verificado que las 10 variables oficiales de relevancia (age, fh, cspca, pirads, vol, psa, comorbidity, psad, dre, bx) están todas en `main_tabular.csv`.
  - Se ignoran intencionalmente para este experimento: `psa_tr_*`, `lab_*`, resto de `txt_*`, `txt_consolidated_ehr_narrative`.
  - Validaciones duras pasadas: shapes exactos, 195 filas/`case_id` único, mismo conjunto de casos, 10 variables presentes, sin duplicados entre vistas, sin fuga de `ground_truth`, redondeo byte-fiel, 4 casos sin MRI con all-NaN en `images.csv`, determinismo, y `ground_truth.csv`/`mccv_loocv_splits.csv` byte-idénticos.
  - `DESIGN.md`/`IMPLEMENTATION.md` reescritos; fila de `experiments/INDEX.md` actualizada; `results/` regenerado (validation_report all_passed, data_manifest con SHA-256, git_commit.txt).
- **Next Tasks:** Entrenar los tres modelos (decisión yes/no, confianza clínica ordinal, 10 pesos de relevancia) sobre `main_tabular` + `full_prompt_narrative` + `images` con nested CV; convertir relevancia predicha (peso ≥ `noted`) a `reveal_sequence` mediante diccionario por grupo (radiology_report←pirads/psad/cspca/vol; laboratory_results←dre; family_history←fh; pathology_report←bx; psa_trend←psa).

## [2026-08-16] - Leak-safe Visibility Views & Reveal-Group Feature Manifold (exp_3)
- **Current Project State:** exp_3 implementado y verificado. `inputs.csv` (195×1077) queda particionado en 7 vistas de evidencia alineadas con la semántica de `reveal_sequence` del evaluador + un manifiesto de procedencia en `data/chimera26/preprocessed/task1/`. `ground_truth.csv` y `mccv_loocv_splits.csv` permanecen byte-idénticos.
- **Specific Advancements:**
  - Contrato de datos por **visibilidad** (no por significado clínico): `main.csv` (195×34, 33 features visibles de `structured-prompt.json`, incluye `path_hist_*`); `images.csv` (195×1025, embeddings MRI como modalidad externa; **NO** forma parte del `reveal_sequence` oficial); 5 grupos-tool mapeados 1:1 a los nombres oficiales del evaluador (`radiology_report`, `laboratory_results`, `psa_trend`, `previous_notes`, `family_history`).
  - `feature_manifest.csv` (1077×11): una fila por feature con `data_type`, `raw_source_file`, `raw_source_field`, `visibility_group`, `official_reveal_name`, flags `is_main`/`is_image`/`is_tool_revealed`/`included_in_model_views` y notas.
  - `txt_consolidated_ehr_narrative` registrada como `derived_excluded` (concatena fuente visible + oculta → riesgo de fuga) y excluida de todas las vistas de modelado.
  - Caveat documentado: `cli_dre` visible en `main`, pero el evaluador la asocia a `laboratory_results` para `section_grounding_score`.
  - Script `experiments/exp_3/scripts/build_grouped_views.py` con 10 validaciones duras: 195 filas/`case_id` único por vista, mismo conjunto de casos, unión de grupos == columnas de `inputs.csv` sin duplicados, sin fuga de `ground_truth`, redondeo byte-fiel, 4 casos sin MRI con filas all-NaN en `images.csv`, determinismo (doble corrida idéntica), shapes esperados y hashes SHA-256 inmutables.
  - Artefactos: `experiments/exp_3/results/validation_report.json` (all_passed), `results/data_manifest.csv` (SHA-256 de 11 archivos), `results/git_commit.txt` (`702fc02`), `DESIGN.md` e `IMPLEMENTATION.md`; fila en `experiments/INDEX.md`.
- **Next Tasks:** Entrenar el `tool_selector` multilabel sobre `main` (política observada, no causal); ensemble de decisión sobre `main` + tools seleccionadas + predictor de imagen con `p(yes)` calibrada → `confidence`; modelo de relevancia ordinal de 10 variables; retrieval de vecinos solo con entrenamiento-folds (nested CV: inner MCCV dentro de cada fold LOOCV para corregir la falta de independencia estricta del LOOCV de exp_2).

## [2026-08-07] - Reestructuracion Canonica y Extraccion del Dataset Maestro (exp_1)
- **Current Project State:** Las fuentes crudas de la Tarea 1 (`structured-prompt.json`, `prostate-biopsy-decision-clinical-data.json`, `prostate-modality-level-neural-representations.json` y JSONs de etiquetas) se han reestructurado y extraído hacia un dataset canónico maestro limpio y uniforme sin colecciones en `data/chimera26/preprocessed/task1/`.
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental en [experiments/exp_1/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_1/DESIGN.md) e [experiments/exp_1/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_1/IMPLEMENTATION.md).
  - Creada la librería extractora [src/data/build_master_dataset.py](file:///home/jmalagont/project/pathology-reasoning/src/data/build_master_dataset.py) y ejecutado el script [experiments/exp_1/scripts/run_preprocessing.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_1/scripts/run_preprocessing.py).
  - Generado `inputs.csv` ($195 \times 1077$) conteniendo todas las variables clínicas escalares (`cli_`), constantes vitales aplanadas de `vitals` (`vit_`), dinámica longitudinal de PSA (`psa_tr_`), laboratorios (`lab_`), biopsia previa (`path_hist_`), 13 columnas de texto extenso (`txt_*`) y 1024 columnas flotantes de la resonancia (`mri_emb_`).
  - Generado `ground_truth.csv` ($195 \times 27$) conteniendo las etiquetas de decisión (`target_biopsy_decision`), confianza (`target_confidence`), los 10 pesos de relevancia oficial (`target_weight_*` y `target_code_weight_*`), texto libre y secuencias de herramientas.
  - Auditado y validado en [experiments/exp_1/results/validation_report.json](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_1/results/validation_report.json) con cero columnas de colecciones o listas y paridad total en las 10 variables objetivo.
- **Next Tasks:** Diseñar e implementar modelos baseline y clasificadores multimodales sobre las matrices tabulares maestras recién construidas.

## [2026-08-09] - Clean Cohort Selection & Rigorous Validation Protocol (exp_2, diseño aprobado)
- **Current Project State:** Diseño del protocolo de cohorte limpia y de validación MCCV + LOOCV aprobado y documentado en [experiments/exp_2/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_2/DESIGN.md) sobre los datasets maestros de `data/chimera26/preprocessed/task1/`. Estado: diseño registrado; la ejecución (generación y verificación de splits, artefactos en `results/`) queda pendiente.
- **Specific Advancements:**
  - Criterios de exclusión: casos sin etiquetas (`unlabeled_test`, N=102), sin embeddings MRI (`excluded_missing_mri`, N=4: `PT-pseudo_4bfd4ec864d8`, `PT-pseudo_4d54f04e26ae`, `PT-pseudo_7dbdcd6f9064`, `PT-pseudo_8636aa471ef7`) y sin PI-RADS (`excluded_missing_pirads`, N=1: `PT-pseudo_3646e0a2ae13`). Cohorte limpia **`usable_labeled` N=88** (45.1%).
  - Política de imputación downstream: `cli_bx` como categoría válida `"No Prior Biopsy"`; `cli_fh` como `"Unknown / 0"`.
  - Protocolo MCCV (selección): 50 repeticiones estratificadas por `target_biopsy_decision_binary` (80% train / 20% val) para selección de hiperparámetros por Macro-F1.
  - Protocolo LOOCV (evaluación): 88 folds con hiperparámetros congelados de MCCV para predicciones out-of-fold sin fuga.
  - Artefacto permanente previsto: `data/chimera26/preprocessed/task1/mccv_loocv_splits.csv` ($195 \times 56$): `case_id`, `cohort_status`, `has_gt`, `has_mri`, `has_pirads`, `loocv_fold`, `mccv_split_00` … `mccv_split_49`. Seed fija 42.
  - Pendiente de verificación: `experiments/exp_2/IMPLEMENTATION.md`, script de generación de splits y `mccv_loocv_splits.csv` no están presentes en el workspace actual.
- **Next Tasks:** Implementar el script de generación de splits y verificar `mccv_loocv_splits.csv` (dimensiones 195×56, conteos de cohorte 88/102/4/1, estratificación y seed 42); a partir de ahí ejecutar los baselines sobre la cohorte limpia.

## 2026-08-16 - exp_2: Clean Cohort Selection & Rigorous Validation Protocol implementado y verificado

- **Current Project State:** exp_2 ejecutado y verificado. El script `experiments/exp_2/scripts/generate_splits.py` regenera de forma determinista `data/chimera26/preprocessed/task1/mccv_loocv_splits.csv` (195×56) a partir de `inputs.csv` y `ground_truth.csv`, con validación dura de todas las reglas del diseño.
- **Specific Advancements:**
  - Flags derivados de los datos (sin asumir IDs): `has_gt`=91, `has_mri`=191, `has_pirads`=194; cohorte `usable_labeled` N=88 (54 yes / 34 no), `unlabeled_test` 102, `excluded_missing_mri` 4, `excluded_missing_pirads` 1 — coinciden exactamente con las listas del DESIGN.
  - Jerarquía de exclusión aplicada (MRI → PI-RADS → unlabeled → usable): los 2 casos sin MRI sin etiquetas (`PT-pseudo_7dbdcd6f9064`, `PT-pseudo_8636aa471ef7`) se clasifican como `excluded_missing_mri`, no como `unlabeled_test`.
  - MCCV: 50 splits estratificados (80/20, seed 42); todos 70/18 con ambas clases (43/27 train, 11/7 val).
  - LOOCV: folds 0–87 en orden lexicográfico por `case_id`; excluidos = -1 en `loocv_fold` y en todos los `mccv_split_*`.
  - Determinismo verificado (doble corrida idéntica); artefacto regenerado byte-idéntico al preexistente y SHA-256 `eda3391e…` coincide con los manifiestos históricos de exp_3/exp_4.
  - Artefactos: `mccv_loocv_splits.csv`, `results/validation_report.json` (all_passed), `results/data_manifest.csv` (SHA-256 inputs/gt/splits), `results/git_commit.txt` (`702fc02`), `reports/summary.md`; `IMPLEMENTATION.md` escrito.
  - Corrección documental en DESIGN.md §2.1: shape de `inputs.csv` (195×1025 → 195×1077, verificado contra `experiments/exp_1/results/validation_report.json`); checklist de reproducibilidad completado.
- **Next Tasks:** Ejecutar baselines sobre la cohorte limpia de 88 con `mccv_loocv_splits.csv` (KNN tabular 39 variables); sincronizar el remoto (`origin/dev`) con los commits pendientes.

## [2026-08-17] - exp_12 ejecutado: fusión tardía multimodal T+M+X ganadora
- **Current Project State:** Fusión tardía multimodal completada. Mejor combinación: T+M+X (tabular+MRI+texto). Ejecutado en tmux session 0. Artefactos escritos en `experiments/exp_12/results/`.
- **Specific Advancements:**
  - **Ganador**: `fusion_T+M+X` (fusión promedio de las 3 modalidades).
  - **MCCV**: F1_macro=0.704, F1_yes=0.795, balanced_acc=0.704, MCC=0.440, Brier=0.190, specificity=0.563, sensitivity=0.845, ROC-AUC=0.768.
  - **LOO**: F1_macro=0.768, F1_yes=0.829, balanced_acc=0.764, MCC=0.539, Brier=0.180, specificity=0.676, sensitivity=0.852, ROC-AUC=0.792.
  - **Comparación con modalidades individuales (MCCV)**: T=0.667, M=0.579, X=0.646, T+M=0.657, T+X=0.665, M+X=0.617, T+M+X=0.704. La fusión completa supera a cualquier modalidad individual o par.
  - **Diversidad**: correlación probabilística T↔M=-0.057 (casi independientes), T↔X=0.216, M↔X=0.005. Dis agreement rates ~40-45% entre pares. Esto justifica la fusión por promedio.
  - **Configuración**: Tabular (exp_5): tau=0.60, k=1, cosine, uniform, cw, 21 vars intersectadas. MRI (exp_9): PCA n=1, k=1, euclidean, distance, cw. Texto (exp_10 corrected): max_features=2000, k=3, cosine, distance, cw.
  - **Pruning fix aplicado**: `apply_pruning` ahora descarta variables con >50% missingness antes de calcular la matriz de asociación, alineando con exp_5. Intersección de 21 variables verificada.
  - **Coste computacional**: 0.9 min total (150 trainings MCCV + 264 LOO folds).
- **Next Tasks:** Analizar si T+M+X supera significativamente a T solo en LOO. Documentar hallazgos. Evaluar siguiente paso: ¿exp_13 (optimización de pesos en fusión) o transición a Pipeline B?

## [2026-08-17] - exp_13 ejecutado: fusión tardía con pesos aprendidos
- **Current Project State:** Fusión tardía con pesos ejecutada. Rejilla simplex de 231 candidatos. Mejor config: T+M+X con w=(0.30, 0.30, 0.40). Artefactos en `experiments/exp_13/results/`.
- **Specific Advancements:**
  - **Ganador**: `fusion_T+M+X_w0.30_0.30_0.40`. Pesos: T=0.30, M=0.30, X=0.40.
  - **MCCV**: F1_macro=0.697, F1_yes=0.788, balanced_acc=0.697, MCC=0.422, Brier=0.193, specificity=0.560, sensitivity=0.835, ROC-AUC=0.765.
  - **LOO**: F1_macro=0.758, F1_yes=0.818, balanced_acc=0.755, MCC=0.516, Brier=0.184, specificity=0.676, sensitivity=0.833, ROC-AUC=0.782.
  - **Top 5 MCCV**: (1) w=(0.30,0.30,0.40) F1m=0.697, (2) w=(0.25,0.35,0.40) F1m=0.694, (3) w=(0.30,0.35,0.35) F1m=0.691, (4) w=(0.35,0.20,0.45) F1m=0.691, (5) w=(0.35,0.35,0.30) F1m=0.691.
  - **Comparación con fusión uniforme (exp_12)**: exp_12 F1m=0.704 MCCV, 0.768 LOO. exp_13 F1m=0.697 MCCV, 0.758 LOO. **No hay mejora sobre el promedio uniforme.** La diferencia probablemente se debe al cambio en el contrato tabular (21 fijos sin pruning fold-local vs. 21 interseccionados con pruning fold-local).
  - **Pesos del top 10**: Todos son T+M+X (sin anulación de modalidades). Pesos en rango T=[0.20,0.45], M=[0.20,0.45], X=[0.10,0.45]. Texto recibe el mayor peso en la mayoría de configs top.
  - **Contrato tabular fijo**: 21 variables de exp_5 sin rerun de correlation pruning. Esto produce predicciones tabulares ligeramente diferentes a exp_12.
  - **Coste computacional**: 4.4 min total (150 trainings MCCV + 264 LOO folds).
- **Next Tasks:** Comparar exp_12 vs exp_13 en el mismo contrato tabular para aislar el efecto de los pesos. Evaluar si el cambio tabular (pruning fold-local vs fijo) es el driver de la diferencia.

## [2026-08-17] - exp_14 ejecutado: ICI para predicción de confianza clínica (Task 1.2)
- **Current Project State:** Predicción de `target_confidence` (uncertain/borderline/clear) usando ICI + árbol ordinal monotónico. 50 MCCV splits con inner OOF para训练 del árbol. Resultado: hipótesis NO respaldada.
- **Specific Advancements:**
  - **Descriptor ICI**: $ICI_i = 2|\bar{p}_i - 0.5| \cdot (1 - 2\sigma_i)$, donde $\bar{p}_i$ es la media de probabilidades T/M/X y $\sigma_i$ es la desviación estándar poblacional.
  - **Modelo**: Árbol ordinal monotónico (regression tree, max_depth=2, max_leaf_nodes=3, min_samples_leaf=5) con 2 umbrales aprendidos por fold.
  - **Protocolo**: 50 MCCV splits, inner 3-fold OOF para训练 del árbol (sin fuga), evaluación en val de cada split.
  - **Resultado ICI tree** (media 50 folds): acc=0.380±0.131, F1_macro=0.286±0.097, OD=0.427±0.124, QWK=0.097±0.197.
  - **Baseline mayoritaria** (siempre 'clear'): acc=0.650, F1_macro=0.263, OD=0.248, QWK=0.000.
  - **Diagnóstico ICI**: Spearman(ICI, target_confidence) = 0.142 (p<0.0001), estadísticamente significativo pero débil. Medias ICI: clear=0.254, borderline=0.198, uncertain=0.174 — tendencia correcta pero con solapamiento masivo.
  - **Umbrales** (mediana over folds): t1=0.048 (uncertain/borderline), t2=0.321 (borderline/clear). Rangos amplios indican inestabilidad.
  - **Veredicto**: OD=0.427 >> 0.248 (baseline). El árbol ICI produce PEORES predicciones ordinales que predecir siempre 'clear'. QWK=0.097 ≈ 0 (chance). La debilidad de la correlación ICI↔confianza (0.142) impide que el árbol discrimine entre categorías.
  - **Coste**: 1.5 min total.
- **Next Tasks:** Task 1.2 sigue sin solución. Considerar: (1) usar ICI como feature de un modelo más complejo (no solo árbol de 1 variable), (2) usar probabilidades individuales T/M/X como features, (3) separar los3 modelos de pipeline (decisión, confianza, relevancia) en lugar de usar ICI como proxy. También evaluar Task 1.3 (relevancia clínica).

## [2026-08-17] - exp_14 corregido: Ordinal Logistic para confianza clínica (Task 1.2)
- **Current Project State:** Predicción de `target_confidence` (uncertain/borderline/clear) usando `mord.LogisticAT(alpha=1.0)` directamente sobre las 21 variables tabulares congeladas de exp_5. Sin ICI, sin modelos base, sin inner CV, sin confidence_weighted. 50 MCCV splits.
- **Specific Advancements:**
  - **Modelo corregido**: Regresión logística ordinal proporcional (All-Threshold) con 51 features (21 vars + indicadores de ausencia + OHE). Alpha=1.0 fijo.
  - **Protocolo**: 50 splits MCCV (70/18), preprocesamiento fit-on-train, sin inner CV.
  - **Resultado** (media 50 folds): acc=0.559±0.106, F1_macro=0.362±0.113, OD=0.254±0.066, QWK=0.270±0.180, Brier=0.496±0.095, AUC=0.646±0.208.
  - **Baseline** (siempre 'clear'): acc=0.650, F1_macro=0.263, OD=0.248, QWK=0.000, Brier=0.700, AUC=0.500.
  - **Convergencia**: 50/50 folds convergieron.
  - **Diagnóstico Spearman**: 0.295±0.205 (vs ICI 0.142 — mejor correlación ordinal).
  - **Per-class**: F1_clear=0.735, F1_borderline=0.219, F1_uncertain=0.110. Recall_borderline=0.293, Recall_uncertain=0.115. Minorías siguen siendo débiles.
  - **Veredicto**: El modelo ordinal supera al baseline en QWK (+0.270), F1_macro (+0.099), Brier (-0.204), AUC (+0.146). PERO: OD=0.254 > 0.248 (baseline), es decir, el modelo produce predicciones ordinalmente PEORES que predecir siempre 'clear' por un margen estrecho (+0.006). La accuracy disminuye (0.559 vs 0.650) porque el modelo intenta predecir minorías pero falla la mayoría de las veces.
  - **Coste**: 0.1 min total (50x más rápido que exp_14 original).
- **Next Tasks:** Task 1.2 sigue sin superar al baseline en OD. Considerar: (1) weighted ordinal loss para penalizar más los errores entre uncertain↔clear, (2) class weights para compensar desbalance, (3) features adicionales (probabilidades T/M/X del ensamble como input del modelo de confianza), (4) aceptar que la confianza clínica no es predecible con features tabulares y transicionar a Task 1.3.

## [2026-08-17] - exp_14 (ICI tree + dual-axis evaluation)
- **Current Project State:** Predicción de `target_confidence` usando DecisionTreeClassifier sobre ICI. Evaluación dual: métricas poblacionales (OE, QWK) + balanceadas por clase (macro-ordinal score, balanced accuracy, macro-F1). class_weight como hiperparámetro: None vs "balanced".
- **Specific Advancements:**
  - **Framework de evaluación dual**: Se reemplazó la selección basada solo en OD por una regla que combina: validez estructural 100%, F1m > baseline, BalAcc > baseline, maximizar macro-ordinal_score, tiebreak con F1m/BalAcc/min_recall_minority. Se excluyen modelos que colapsan minorías.
  - **Métricas añadidas**: macro_ordinal_error = (1/3)Σ_c (1/n_c) Σ_{i:y_i=c} |ŷ_i - y_i|/2; macro_ordinal_score = 1 - macro_ordinal_error; balanced accuracy; per-class recall/F1/precision; error_unclear (uncertain/borderline → clear); error_reverse (clear → uncertain/borderline); PR-AUC para low-confidence.
  - **Resultados MCCV** (media 50 folds):
    - `tree_none` (sin balanceo): OE=0.270±0.086, macOS=0.555, F1m=0.312, BalAcc=0.371, rec_unc=0.108, rec_brd=0.072, min_rec=0.000.
    - `tree_balanced` (con balanceo): OE=0.439±0.172, macOS=0.561, F1m=0.269, BalAcc=0.340, rec_unc=0.320, rec_brd=0.269, min_rec=0.125.
    - Baseline: OE=0.261, macOS=0.500, F1m=0.259, BalAcc=0.333.
    - Selección: `tree_balanced` gana por macro_ordinal_score (0.561 > 0.555). Ambos pasan filtros (F1m > baseline, BalAcc > baseline, 100% validez).
  - **Resultados LOO** (`tree_balanced`): OE=0.4148 (95% CI: [0.330, 0.500]), macOS=0.6171 (95% CI: [0.526, 0.703]), QWK=0.139, F1m=0.368, BalAcc=0.425, rec_unc=0.643, rec_brd=0.167, rec_clr=0.464, valid=88/88.
  - **Salidas generadas**: summary.json, per_fold.csv, predictions_mccv.csv, predictions_loo.csv, confusion_matrices.json, evaluation_scorecard.csv, 5 figuras de matrices de confusión.
  - **Coste**: 5.5 min total.
- **Next Tasks:** (1) Analizar matrices de confusión y transiciones de error, (2) Considerar si el tradeoff OE↔macOS es aceptable para la aplicación clínica, (3) Explorar si features del ensamble mejoran la separación, (4) Transicionar a Task 1.3.

## [2026-08-17] - exp_15 ejecutado: vector 4D [ICI, p_T, p_M, p_X] para predicción de confianza
- **Current Project State:** Predicción de `target_confidence` (uncertain/borderline/clear) con DecisionTreeClassifier(max_depth=2, max_leaf_nodes=3) sobre 3 familias de features: ici_only [ICI], p_only [p_T, p_M, p_X], ici_plus_p [ICI, p_T, p_M, p_X]. Cada familia × class_weight ∈ {None, "balanced"} = 6 configs. Evaluación dual-axis (poblacional + balanceada). 50 MCCV + LOO.
- **Specific Advancements:**
  - **Familia ganadora en MCCV**: `ici_only_balanced` (macOS=0.5607 > `ici_only_none` 0.5552). En p_only y ici_plus_p, `balanced` también gana.
  - **MCCV resultados**:
    - `ici_only_none`: macOS=0.5552, F1m=0.312, BalAcc=0.371, feat={ICI:100%}
    - `ici_only_balanced`: macOS=0.5607, F1m=0.269, BalAcc=0.340, feat={ICI:100%}
    - `p_only_none`: macOS=0.5496, F1m=0.295, BalAcc=0.360, feat={ICI:51%, p_T:16%, p_M:33%, p_X:0%}
    - `p_only_balanced`: macOS=0.5914, F1m=0.287, BalAcc=0.366, feat={ICI:40%, p_T:20%, p_M:40%, p_X:0%}
    - `ici_plus_p_none`: macOS=0.5500, F1m=0.297, BalAcc=0.350, feat={ICI:31%, p_T:42%, p_M:6%, p_X:21%}
    - `ici_plus_p_balanced`: macOS=0.5827, F1m=0.272, BalAcc=0.340, feat={ICI:30%, p_T:32%, p_M:11%, p_X:27%}
    - Baseline: OE=0.261, macOS=0.500, F1m=0.259, BalAcc=0.333
  - **LOO resultados** (ganadores por familia):
    - `ici_only_balanced`: OE=0.4148 (CI:[0.330,0.506]), macOS=0.6171 (CI:[0.524,0.702]), QWK=0.139, F1m=0.368, BalAcc=0.425, rec_unc=0.643, rec_brd=0.167, feat={ICI:100%}
    - `p_only_balanced`: OE=0.3068 (CI:[0.244,0.375]), macOS=0.6157 (CI:[0.553,0.677]), QWK=0.206, F1m=0.318, BalAcc=0.345, rec_unc=0.000, rec_brd=0.444, feat={ICI:48.3%, p_T:2.8%, p_M:48.9%, p_X:0.0%}
    - `ici_plus_p_balanced`: OE=0.3239 (CI:[0.256,0.392]), macOS=0.6042 (CI:[0.540,0.665]), QWK=0.127, F1m=0.308, BalAcc=0.345, rec_unc=0.000, rec_brd=0.500, feat={ICI:10.2%, p_T:47.2%, p_M:1.1%, p_X:41.5%}
  - **Conclusión**: La familia 4D [ICI,p_T,p_M,p_X] NO mejora sobre ICI-only. ΔmacOS(4D vs ICI-only) = -0.013, ΔOE = -0.091. p_only es competitivo en OE (0.307 vs 0.415) pero no detecta uncertain (rec_unc=0.000). ici_plus_p usa p_T y p_X como splits principales (47% y 41%), marginalizando ICI (10%).
  - **Feature usage notable**: En p_only y ici_plus_p con balanced, el árbol ignora p_X en p_only (0% splits) pero lo usa fuertemente en ici_plus_p (41.5%). ICI es el split dominante en ici_only (100%) y tiene uso mixto en las familias 4D.
  - **Salidas**: summary.json, per_fold.csv, predictions_mccv.csv, predictions_loo_{config}.csv, evaluation_scorecard.csv, 9 figuras de matrices de confusión (3 MCCV + 6 LOO counts/norm).
  - **Coste**: 15.2 min total.
- **Next Tasks:** (1) Decidir si el tradeoff OE↔macOS de ici_only_balanced es aceptable, (2) Considerar que p_only tiene mejor OE pero no detecta uncertain, (3) Evaluar si Task 1.2 requiere features clínicas tabulares directas en vez de probabilidades de modalidades, (4) Transicionar a Task 1.3 (relevancia clínica).

## [2026-08-17] - exp_16 ejecutado: ICI regression tree para confianza clínica
- **Current Project State:** Predicción de `target_confidence` usando `DecisionTreeRegressor` sobre ICI con label encoding ordinal (uncertain=0, borderline=1, clear=2). Tres condiciones: reg_l1_none, reg_l1_balanced, reg_l2_balanced. Prediction conversion via `clip(floor(score+0.5), 0, 2)`.
- **Specific Advancements:**
  - **MCCV resultados**:
    - `reg_l1_none` (absolute_error, sin pesos): macOS=0.5655, F1m=0.314, BalAcc=0.364, monotonic_rate=0.32
    - `reg_l1_balanced` (absolute_error, pesos balanceados): macOS=0.6380, F1m=0.191, BalAcc=0.320, monotonic_rate=0.34
    - `reg_l2_balanced` (squared_error, pesos balanceados): macOS=0.6639, F1m=0.168, BalAcc=0.354, monotonic_rate=0.20
    - Baseline: OE=0.261, macOS=0.500
  - **Selección MCCV**: `reg_l1_none` gana por tener mejor F1m (0.314) entre las configs válidas.
  - **LOO** (`reg_l1_none`): OE=0.2670 (CI:[0.199,0.341]), macOS=0.5605 (CI:[0.509,0.617]), QWK=0.112, F1m=0.304, BalAcc=0.335, rec_unc=0.000, rec_brd=0.167, valid=88/88, monotonic_rate=0.00.
  - **Diagnóstico**: El regresor sin balanceo casi siempre predice `clear` (leaf values [2.0, 2.0, 1.0]), similar al baseline. Con balanceo, macOS sube en MCCV (0.638, 0.664) pero F1m colapsa (0.19, 0.17) porque el modelo predice clases minoritarias incorrectamente. La monotonicidad es muy baja (0.20-0.34), indicando que las hojas no mantienen orden ordinal.
  - **Comparación con exp_15**: LOO macOS=0.561 vs exp_15 `ici_only_balanced` macOS=0.617. La regresión es **inferior** al clasificador Gini. rec_unc=0.000 vs exp_15 0.643.
  - **Coste**: 7.1 min total.
- **Next Tasks:** (1) La regresión ordinal sobre ICI no mejora al clasificador Gini, (2) Task 1.2 está limitada por la débil señal ICI↔confianza independientemente del criterio de optimización, (3) Evaluar si features tabulares directas o un modelo más complejo puede mejorar, (4) Transicionar a Task 1.3 (relevancia clínica).

## [2026-08-18] - Corrección exp_14/15/16: ConfidenceWeightedKNN + y_binary (corrección final)
- **Current Project State:** Re-ejecución final de exp_14, exp_15, exp_16 con corrección crítica: (1) base models usan `ConfidenceWeightedKNN` (no `StandardKNN`), (2) KNNs entrenan en `target_biopsy_decision_binary` (no `target_confidence`). Protocolo canónico: MOE_abs primario, F1_macro desempate. Código autocontenido, sin módulos compartidos.
- **Specific Advancements:**
  - **Corrección crítica aplicada**: Todos los 3 runners reescritos para usar `ConfidenceWeightedKNN` entrenado en `y_binary` con `conf_numeric` como pesos de vecinos, alineado con los ganadores exp_4–exp_11 y exp_12.
  - **Protocolo canónico**: MOE_abs = (1/3) * Σ_c mean_{i:y_i=c}(|pred_i - true_i|/2). Rango [0,1]; 0=perfecto, 0.5=baseline. Selección: validez 100%, MOE_abs < baseline, sin recall cero, minimizar MOE_abs, desempate F1_macro.
  - **exp_14 resultados finales** (v4):
    - `tree_none`: MOE_abs=0.4742, F1m=0.3114
    - `tree_balanced` (SELECTED): MOE_abs=0.4064, F1m=0.2976
    - Baseline: MOE_abs=0.5000, F1m=0.2626
    - LOO tree_balanced: MOE_abs=0.4160, F1m=0.2230
  - **exp_15 resultados finales** (v2):
    - `ici_only_balanced`: MCCV MOE_abs=0.4064, LOO MOE_abs=0.4160
    - `p_only_balanced`: MCCV MOE_abs=0.4006, LOO MOE_abs=0.3770
    - `ici_plus_p_balanced`: MCCV MOE_abs=0.4123, LOO MOE_abs=0.4282
    - Baseline: MOE_abs=0.5000
    - **Nota**: exp_15 fue implementado como classifier (3 familias × class_weight = 6 configs), NO como regressor aprobado originalmente.
  - **exp_16 resultados finales** (v2):
    - `reg_l1_none`: MOE_abs=0.4746, F1m=0.3028
    - `reg_l1_balanced`: MOE_abs=0.3615, F1m=0.2084
    - `reg_l2_none`: MOE_abs=0.4095, F1m=0.2950
    - `reg_l2_balanced` (SELECTED): MOE_abs=0.3480, F1m=0.1838
    - LOO reg_l2_balanced: MOE_abs=0.3581, F1m=0.1328
    - Baseline: MOE_abs=0.5000
  - **Coste total**: ~35 min (exp_14: 5.9 min, exp_15: 18.7 min, exp_16: 10.2 min).
- **Next Tasks:** (1) Análisis comparativo de los 3 experimentos bajo el mismo protocolo, (2) Determinar si algún modelo supera significativamente al baseline, (3) Decidir si la señal ICI↔confianza es suficiente para Task 1.2, (4) Transicionar a Task 1.3 si es necesario.

## [2026-08-18] - Selección pendiente: exp_14/tree_balanced como mejor modelo para subtask 1.2
- **Current Project State:** Subtask 1.2: resultados corregidos disponibles (ConfidenceWeightedKNN + y_binary). Selección pendiente de validación del usuario tras corrección completa de los 3 experimentos.
- **Specific Advancements:**
  - **Tabla comparativa MCCV (N=900)**:
    | Modelo | Config | MOE_abs ↓ | F1_macro ↑ |
    |--------|--------|-----------|------------|
    | full clear | always clear | 0.5000 | 0.2626 |
    | exp_14 | tree_balanced | 0.4064 | 0.2976 |
    | exp_15 | p_only_balanced | **0.4006** | **0.3172** |
    | exp_16 | reg_l2_balanced | **0.3480** | 0.1838 |
  - **Tabla comparativa LOO (N=88)**:
    | Modelo | Config | MOE_abs ↓ | F1_macro ↑ |
    |--------|--------|-----------|------------|
    | full clear | always clear | 0.5000 | 0.2626 |
    | exp_14 | tree_balanced | 0.4160 | 0.2230 |
    | exp_15 | p_only_balanced | **0.3770** | **0.3633** |
    | exp_16 | reg_l2_balanced | 0.3581 | 0.1328 |
  - **Nota importante**: exp_15 fue implementado como classifier (no regressor como aprobado originalmente); exp_16 implementa el diseño regressor original.
- **Next Tasks:** (1) Evaluar si el modelo seleccionado es suficiente para la tarea, (2) Considerar transición a subtask 1.3 (relevancia clínica), (3) Preparar predicciones finales para el conjunto de test del Challenge.

## [2026-08-18] - exp_18 ejecutado: Optimización Global Exhaustiva de Umbrales Ordinales sobre Riesgo de Decisión (Subtarea 1.2 — ★ Récord de la Campaña)
- **Current Project State:** Subtarea 1.2 resuelta con un nuevo Récord de la Campaña `exp_18` (`c_fn_0.65_lambda_0.00_mode_exact_balanced_min_recall`). Ejecutado en sesión tmux 0 con el entorno `histo-DL`. Resultados, scorecard y figuras guardados en `experiments/exp_18/`.
- **Specific Advancements:**
  - **Optimización Global de Umbrales:** En lugar de reglas por árbol de decisión (`exp_17`), `exp_18` evalúa vectorialmente en NumPy las $2,415$ combinaciones posibles de umbrales $(\tau_1, \tau_2)$ en el escalar de riesgo $\Omega(c_{\text{fn}}, \lambda)$ y selecciona en train el par exacto que minimiza el Error Ordinal Balanceado ($\text{MOE}_{\text{abs}}$) y maximiza $\text{F1}_{\text{macro}}$.
  - **Rejilla de 50 Condiciones:** $c_{\text{fn}} \in \{0.20, 0.35, 0.50, 0.65, 0.80\} \times \lambda \in \{0.00, 0.25, 0.50, 0.75, 1.00\} \times \text{threshold\_mode} \in \{\text{"exact\_free"}, \text{"exact\_balanced\_min\_recall"}\}$.
  - **Configuración Ganadora:** `c_fn_0.65_lambda_0.00_mode_exact_balanced_min_recall` ($c_{\text{fn}}=0.65, c_{\text{fp}}=0.35, \lambda=0.00$, recall mínimo 3 preds por clase en train).
  - **MCCV Promedio ($N=900$):** $\text{MOE}_{\text{abs}}^{\text{mean}} = \mathbf{0.3183 \pm 0.0665}$ (caída drástica vs `exp_17` $0.3949$ y vs baseline $0.5000$). $\text{MOE}_{\text{abs}}^{\text{pooled}} = 0.3315$, $\text{F1}_{\text{macro}}^{\text{pooled}} = 0.3157$.
  - **LOO Out-Of-Fold ($N=88$):** $\text{MOE}_{\text{abs}}^{\text{LOO}} = \mathbf{0.3089}$ (caída de $-0.0707$ vs `exp_17`), $\text{F1}_{\text{macro}}^{\text{LOO}} = \mathbf{0.4078}$ (**Rompe la barrera de $0.40$ por primera vez en Subtarea 1.2**).
  - **Detección por Clases en LOO:** `uncertain` = $50.0\%$ ($7/14$), `borderline` = $61.1\%$ ($11/18$), `clear` = $28.6\%$ ($16/56$). Evita por completo el colapso de clases minoritarias.
- **Next Tasks:** Transición formal a la Subtarea 1.3 (Predicción de Pesos de Relevancia Clínica para las 10 variables oficiales y derivación de `reveal_sequence`).

## [2026-08-19] - Arquitectura Completa de Despliegue de Herramientas para Agentes (deploy/subtask_1.1, 1.2, 1.3)
- **Current Project State:** Las tres subtareas de CHIMERA Tarea 1 han sido empaquetadas en módulos de despliegue autocontenidos dentro de `deploy/`, listos para ser invocados como Tools por Agentes de IA.
- **Specific Advancements:**
  - **Subtarea 1.1 (`deploy/subtask_1.1/`):**
    - `train_and_save.py`: Entrena la fusión multimodal tardía (`exp_12`) sobre los $N=88$ casos de la cohorte completa y serializa `model_subtask_1.1.pkl`.
    - `preprocess_input.py`: Preprocesa atributos tabulares, texto narrativo limpio y embedding de resonancia (1024D ➔ 1D PCA).
    - `predict_inference.py`: Retorna la decisión binaria (`RECOMMEND_BIOPSY` vs `NO_BIOPSY`) y las probabilidades por modalidad ($p_T, p_M, p_X, p_{\text{fused}}$).
  - **Subtarea 1.2 (`deploy/subtask_1.2/`):**
    - `compute_risk_descriptor.py`: Calcula la métrica escalar continua de Riesgo de Decisión $\Omega(c_{\text{fn}}=0.65, \lambda=0.00)$.
    - `train_and_save.py`: Optimiza los umbrales 2D globales ($\tau_1^*=0.2438, \tau_2^*=0.8375$) sobre $N=88$ casos y guarda `model_subtask_1.2.pkl`.
    - `predict_inference.py`: Asigna el grado de confianza ordinal (`0=uncertain`, `1=borderline`, `2=clear`).
  - **Subtarea 1.3 (`deploy/subtask_1.3/`):**
    - `compute_shap_descriptors.py`: Extrae valores SHAP locales max-normalizados $\psi_{i, k}$ para las 10 variables clínicas objetivo.
    - `train_and_save.py`: Optimiza 3D umbrales por variable $(\tau_{1, k}^*, \tau_{2, k}^*, \tau_{3, k}^*)$ sobre $N=88$ casos y serializa `model_subtask_1.3.pkl`.
    - `predict_inference.py`: Determina los 10 pesos de relevancia clínica ordinales y mapea la secuencia de evidencias solicitadas (`reveal_sequence`).
- **Next Tasks:** Consolidar el orquestador principal o agente de razonamiento clínico que consuma las herramientas empaquetadas en `deploy/`.

## [2026-08-19] - Agente de Razonamiento Clínico Agnóstico a Herramientas (agent/ & Ollama Integration)
- **Current Project State:** Agente de IA desacoplado implementado en `agent/`, invocando las tools de `deploy/` de forma inmutable y utilizando Pydantic para estructurar las salidas de acuerdo al formato canónico `prostate-biopsy-decision-reasoning.json`.
- **Specific Advancements:**
  - `agent/schemas.py`: Define el modelo Pydantic `PathologyReasoningOutput` con validación estricta de decisiones, niveles de confianza, los 10 pesos ordinales, `reveal_sequence` y `free_text`.
  - `agent/tools.py`: Capa de abstracción agnóstica que desacopla la ejecución de `deploy/subtask_1.1`, `subtask_1.2` y `subtask_1.3`.
  - `agent/ollama_client.py`: Módulo de conexión HTTP local a Ollama para consultar modelos Gemma.
  - `agent/agent_cli.py`: Ejecutable del agente con soporte para archivos `.json` e interfaces CLI. Prompt configurado con rol de **Especialista en Urología** e inmutabilidad de las decisiones de las herramientas de etapa 1.
  - **Inferencia Verificada:** Verificada la inferencia real de `gemma4:e2b` sobre casos positivos (`PT-pseudo_0169468160c6`) y negativos (`PT-pseudo_0020cfca66c8`).
  - **Nota de Infraestructura / GPU Ollama:** Se identificó que Ollama omite la aceleración CUDA en las GPUs GTX TITAN X (Compute Capability 5.2 < 7.5 requerido por binarios precompilados) y pasa a CPU por defecto. Queda registrado para configuración futura con `OLLAMA_VULKAN=true` o compilación customizada.
- **Next Tasks:** Evaluar el pipeline completo sobre el conjunto de prueba o casos nuevos y preparar la entrega.
