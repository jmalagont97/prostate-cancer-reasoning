# Logbook

This hidden logbook tracks approved advancements and next tasks chronologically.
Entries follow the strict format:
```markdown
## [YYYY-MM-DD] - [Advancement Title or Technical Milestone]
- **Current Project State:** [Brief description of the pipeline/model state]
- **Specific Advancements:** [Detail of the changes, code, architecture, or data processed today]
- **Next Tasks:** [Immediate technical next steps]
```

## 2026-08-15 16:45 - exp_3: Tabular KNN Baseline (Task 1.1) re-ejecutado y verificado

- **Current Project State:** exp_3 reconstruido desde cero (el anterior se eliminó y no estaba versionado). Pipeline implementado, ejecutado (128 configs MCCV + LOO de la seleccionada) y verificado; CSV y figuras de matrices de confusión generados. DESIGN.md, IMPLEMENTATION.md, summary.md escritos; INDEX.md actualizado.
- **Specific Advancements:**
  - **Decisiones del PI (registradas en DESIGN.md §2):** selección por **Macro-F1** (desempates F1_yes → balanced accuracy), **sin imputación** (ausencia = 0 estructural + `missing_<var>`; `cli_bx="None"` categoría válida), **rejilla completa de 128 configs** (incluye fuzzy).
  - `experiments/exp_3/scripts/knn_baseline.py`: loader (strings, `keep_default_na=False`), `TabularTransformer` (Min-Max fit-train-only; 36 continuas + 3 categóricas `cli_bx`/`cli_dre`/`vit_smoking_status`; indicadores `missing_*`; assert anti-NaN y anti-norma-cero para cosine), KNN brute-force con argsort estable, `fold_metrics` (incl. ROC/PR con `n_valid_splits`), MCCV con joblib (n_jobs=22), selección, LOO, y 9 CSVs de salida.
  - **MCCV (50 splits congelados, 88 usable, 70/18):** ganador `manhattan_fuzzy_confidence_uniform_k11`, Macro-F1 = **0.6234 ± 0.101**, F1_yes = 0.7418, ROC-AUC = 0.7326, PR-AUC = 0.8346. Supera a `always_yes` (0.3793) y `always_no` (0.2800) en 49/50 y 50/50 folds.
  - **LOO pooled (88 folds, sanity check):** Macro-F1 = **0.6207**, F1_yes = 0.7414, ROC-AUC = 0.6708, PR-AUC = 0.7909. **Sin gap real vs MCCV.**
  - **Corrección metodológica:** el "gap MCCV→LOO = 0.31" de la iteración previa era un artefacto de promediar métricas por-fold a n=1; las métricas LOO pooled son las válidas y coinciden con MCCV.
  - **Fuzzy vs rigid:** empate (mean 0.5702 vs 0.5724); el ganador es fuzzy. Refutada la afirmación previa de "sesgo estructural del voto por confianza".
  - Zona óptima de k = 9–11; cosine peor distancia; inverse_distance sin ventaja sobre uniform.
  - CSVs: `mccv_summary.csv` (128), `mccv_fold_metrics.csv` (6400), `mccv_oof_predictions.csv` (115200), `loo_predictions.csv` (88), `confusion_matrices_mccv.csv` (25600), `confusion_matrix_loo.csv`, `classification_report.csv`, `baseline_metrics.csv`, `data_manifest.csv` (hashes). Figuras: 5 PNG de matrices de confusión (MCCV counts/norm, LOO counts/norm, MCCV-vs-LOO).
  - Datos: `inputs_tabular.csv` = subset exacto de `inputs.csv` (verificado); cohorte 54 yes / 34 no; 3 casos `cli_fh_binary` ausentes sin recuperar (pendiente de decisión del PI).
  - Git commit: `6e4b61c56b6b26a5040f89053756ec4f0c546629` (registrado en `selected_config/git_commit.txt`; árbol no limpio al ejecutar).
  - Entorno: conda `histo-DL`, numpy 1.26.4, pandas 3.0.3, sklearn 1.9.0; runtime MCCV ≈ 41 s.
- **Next Tasks:** Decidir política de umbral de operación (PR-AUC 0.83 sugiere margen); evaluar baseline logístico para estabilidad en N=88; subtareas 1.2/1.3 con el mismo protocolo; recuperar `cli_fh_binary` desde JSON crudos (pendiente PI); reentrenar con los 88 y generar envío al evaluador (102 vs 104, pendiente PI).

## 2026-08-15 - exp_4: Feature-Subset Ablation (>50% missingness) sobre KNN tabular

- **Current Project State:** exp_4 ejecutado y verificado. Se repitió el baseline de exp_3 sobre un subconjunto de 37 variables (filtro pre-registrado `missing_rate > 50%` global sobre los 88 usable_labeled, esenciales siempre retenidas), mismos 50 splits MCCV congelados y 128 configs. Ganador: `manhattan_fuzzy_confidence_inverse_distance_k3`.
- **Specific Advancements:**
  - Filtro: eliminadas `path_hist_bx_gl_tert` (87/88, 98.86%) y `lab_hemoglobin_g_dl` (65/88, 73.86%); retenidas 37 (34 continuas + 3 categóricas). Las 10 esenciales protegidas (ninguna supera 50%; cláusula defensiva permanente). Las 2 eliminadas superan 50% en 50/50 folds de train → máscara global ≈ per-fold en estos datos.
  - MCCV: ganador `manhattan_fuzzy_confidence_inverse_distance_k3`, Macro-F1 = **0.6430 ± 0.110**, F1_yes = 0.7149, balanced acc = 0.6478, ROC-AUC = 0.6792, PR-AUC = 0.7975. Supera a `always_yes` (0.3793) y `always_no` (0.2800).
  - LOO pooled: Macro-F1 = **0.6474** (acc 0.6591, F1_yes 0.7115, F1_no 0.5833, ROC-AUC 0.6863, PR-AUC 0.7861). Sin gap vs MCCV.
  - Comparación pareada (config fija, 50 folds, Wilcoxon): config ganadora de exp_4 mejora 0.5514 → 0.6430 (**+0.0916**, IC95 [0.069,0.114], p=3.96e-08); config ganadora de exp_3 degrada 0.6234 → 0.5934 (−0.030, p=0.0239). **El filtro cambia el óptimo de hiperparámetros** (k=3 + inverse_distance + fuzzy dominan en 37 vars). Ganador-vs-ganador: 0.6430 vs 0.6234 (+0.0196, no prueba formal).
  - Scripts: `knn_baseline.py` (idéntico a exp_3 salvo el filtro + `feature_missingness.csv` + `feature_selection_manifest.csv`), `plot_results.py`, `compare_experiments.py` (CSV + figura scatter 128 configs). 12 CSVs + 6 figuras, counts verificados.
  - Entorno: conda `histo-DL`, MCCV ≈ 39 s. Git commit registrado (mismo árbol no limpio que exp_3).
- **Next Tasks:** Anclar baseline tabular en 37 variables para la campaña; comparar con modelo logístico sobre las 37; subtareas 1.2/1.3; sensibilidad a otros umbrales de filtro (p.ej. 30%) si el PI lo decide; recuperar `cli_fh_binary` (pendiente PI); reentrenar con los 88 y envío al evaluador (pendiente PI).
