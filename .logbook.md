# Logbook

This hidden logbook tracks approved advancements and next tasks chronologically.
Entries follow the strict format:
```markdown
## [YYYY-MM-DD] - [Advancement Title or Technical Milestone]
- **Current Project State:** [Brief description of the pipeline/model state]
- **Specific Advancements:** [Detail of the changes, code, architecture, or data processed today]
- **Next Tasks:** [Immediate technical next steps]
```

## [2026-06-29 15:55] - Robust TIFF File Support and Resolution Metadata Extraction Fallbacks
- **Current Project State:** The pipeline utilizes `large_image` to load and scale WSIs, segment tissue, detect marker pen strokes, extract valid tiles, and run foundation models (UNI2-h/KimiaNet).
- **Specific Advancements:** Modified [utils/wsi_processing/utils.py](file:///home/jmalagont/project/pathology-reasoning/utils/wsi_processing/utils.py) to:
  1. Add a fallback to the `large_image_source_tiff` backend in `open_wsi` if auto-detection fails for `.tiff`/`.tif` extensions.
  2. Implement an internal TIFF resolution parser in `get_wsi_metadata` that extracts standard tags (`tiff.XResolution`, `tiff.YResolution`, `tiff.ResolutionUnit`) to calculate MPP and magnification when high-level fields are missing.
- **Next Tasks:** Perform bulk WSI preprocessing (tissue extraction, tile quality control, and feature extraction) on the PANDA challenge training images, resolving data anomalies (missing masks and grade mismatches).

## [2026-06-29 18:52] - Refactored WSI Processing and Master CLI Script to be Format-Agnostic
- **Current Project State:** The WSI processing package and master CLI scripts are format-agnostic and support generic formats (SVS, TIFF, NDPI, etc.) dynamically. Dynamic downsample sizing rounding bugs have been resolved.
- **Specific Advancements:**
  1. Modified [utils/wsi_processing/utils.py](file:///home/jmalagont/project/pathology-reasoning/utils/wsi_processing/utils.py) to remove extension-specific suffix checking in `open_wsi` and implement an initial standard OpenSlide property fallback (`openslide.mpp-x`/`openslide.mpp-y`) in `get_wsi_metadata`.
  2. Overwrote [utils/extract_wsi_features.py](file:///home/jmalagont/project/pathology-reasoning/utils/extract_wsi_features.py) to load training coordinates, execute dynamic WSI tissue segmentation and marker pen removal, generate 4-panel visual QC reports, run batch inference with `UNI2FeatureExtractor`, and persist embeddings to per-case HDF5 bags.
  3. Modified [utils/run_preprocessing.py](file:///home/jmalagont/project/pathology-reasoning/utils/run_preprocessing.py) to dynamically match all standard WSI extensions (`.svs`, `.tiff`, `.tif`, `.ndpi`) during file ingestion instead of hardcoding `.svs` queries.
  4. Modified [utils/wsi_processing/artifacts.py](file:///home/jmalagont/project/pathology-reasoning/utils/wsi_processing/artifacts.py) to resolve a downsample rounding bug (255x255 vs 256x256 shape discrepancies on non-integer magnification scale factors). Added a tolerance check (up to 2 pixels) and dynamic resizing using PIL to ensure patches are consistently shaped to target dimensions.
- **Next Tasks:** Execute a dry-run test on a subset of 5 PANDA slides to profile extraction speed, VRAM consumption, and tissue segmentation accuracy.

## [2026-06-30] - Agrupamiento de Placas y Pipeline de Razonamiento con K-Means y UNI
- **Estado Actual del Proyecto:** El pipeline de razonamiento patológico utiliza un agrupamiento a nivel de lámina de la cohorte PANDA (10k imágenes de cáncer de próstata a 20X) para asociar placas morfológicamente similares y razonar a partir del texto clínico de casos semejantes.
- **Specific Advancements:**
  - Se diseñó y estructuró el diagrama de flujo del método mostrando las etapas del pipeline: ingesta de PANDA, extracción de características con UNI (embeddings de 1536 dimensiones), agrupamiento K-Means por severidad ISUP y razonamiento clínico con LLM.
  - Monitoreo del pipeline de extracción de características en la sesión tmux 2 (GPU `cuda:1`), logrando procesar y guardar con éxito 860 de las 10,616 láminas totales (~8.1% de avance).
- **Próximas Tareas:** Completar la extracción de características con el modelo UNI, aplicar el agrupamiento K-Means y validar el proceso frente a los grados de severidad ISUP.

## [2026-07-02] - Avance del Pipeline de Extracción de Características UNI2-h sobre PANDA
- **Estado Actual del Proyecto:** El pipeline de extracción de características continúa ejecutándose en la sesión tmux 2 (GPU `cuda:1`) sobre la cohorte PANDA, habiendo alcanzado 3,161 de 10,616 láminas procesadas (~29.8% de avance).
- **Avances Específicos:**
  - Procesadas 3,161/10,616 láminas de la cohorte PANDA con el modelo UNI2-h, generando embeddings de 1,536 dimensiones por parche y almacenándolos en archivos HDF5 por caso.
  - Velocidad promedio de procesamiento estable (~80-125 segundos por lámina dependiendo del tamaño del tejido).
- **Próximas Tareas:** Continuar la extracción de características hasta completar las 10,616 láminas. Posteriormente aplicar K-Means y validar el agrupamiento frente a los grados ISUP.

## [2026-07-09] - Finalización del Pipeline de Extracción de Características UNI2-h sobre PANDA
- **Estado Actual del Proyecto:** El pipeline de extracción de características en la sesión tmux 2 ha finalizado exitosamente el procesamiento completo de la cohorte PANDA (10,612 láminas procesadas). Todos los embeddings morfológicos de 1,536 dimensiones se encuentran persistidos en archivos HDF5.
- **Avances Específicos:**
  - Completado el procesamiento de 10,612 láminas de la cohorte PANDA utilizando el Vision Transformer UNI2-h (100% de avance).
  - Tiempo total acumulado de ejecución del pipeline secuencial: 783,916 segundos (~9.07 días).
  - Todos los embeddings y coordenadas generados y validados.
- **Próximas Tareas:** Aplicar K-Means sobre las características morfológicas agregadas y validar el agrupamiento frente a los grados de severidad ISUP de la cohorte.

## [2026-07-20] - Ingesta, Auditoría de Completitud y Visualización de la Cohorte de la Tarea 1
- **Current Project State:** Se inició el desarrollo sobre el dataset multimodal `Chimera26` enfocado en la Tarea 1 (Decisión de Biopsia). Se completó la auditoría de completitud del dataset y la visualización de fuentes activas para la Tarea 1.
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_1/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_1/DESIGN.md) e [experiments/exp_1/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_1/IMPLEMENTATION.md).
  - Implementado y ejecutado el script [experiments/exp_1/scripts/eda.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_1/scripts/eda.py) dentro del entorno `histo-DL`.
  - Cuantificada la presencia de archivos y la disponibilidad de modalidades en los 195 casos de la Tarea 1, generando [results/eda_metrics.json](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_1/results/eda_metrics.json), reportes visuales con matplotlib en inglés técnico (`file_presence.png` y `active_sources.png`) y el reporte descriptivo [reports/summary.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_1/reports/summary.md).
  - Identificados los 4 casos específicos en la Tarea 1 que carecen de embeddings de resonancia (MRI image).
- **Next Tasks:** Diseñar e implementar el algoritmo de Similitud por Vecinos Más Cercanos ($k$-NN) e indexación híbrida (Embeddings de MRI + Distancia Clínica estructurada) para predecir la decisión de biopsia utilizando los casos del split de entrenamiento (91 casos etiquetados).

## [2026-07-20] - Preprocesamiento y Tabularización Homogénea de la Tarea 1
- **Current Project State:** Las fuentes de información del dataset Chimera26 Tarea 1 se encuentran tabularizadas en 5 archivos CSV consolidados bajo `data/chimera26/preprocessed/task1/`.
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_2/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_2/DESIGN.md) e [experiments/exp_2/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_2/IMPLEMENTATION.md).
  - Implementado y ejecutado el script [experiments/exp_2/scripts/preprocess.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_2/scripts/preprocess.py) en el entorno `histo-DL`.
  - Generados 5 archivos CSV alineados (con exactamente 195 filas ordenadas por ID de paciente): `mri_embeddings.csv` (1024 columnas), `clinical_prompts.csv`, `clinical_data_tabular.csv`, `clinical_reasoning.csv` y `biopsy_decision.csv`.
  - Establecida la codificación de datos faltantes mediante el string `'NONE'` en todos los archivos de manera uniforme.
  - Verificados los datos y guardados los logs en [results/preprocessing_metrics.json](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_2/results/preprocessing_metrics.json) y el reporte [reports/summary.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_2/reports/summary.md).
- **Next Tasks:** Diseñar e implementar el algoritmo de clasificación y similitud híbrido (MRI + clínica estructurada) en `exp_3` utilizando las fuentes tabulares generadas.

## [2026-07-20] - Análisis Exploratorio de Datos Profundo (Deep EDA) de la Tarea 1
- **Current Project State:** Se han caracterizado detalladamente las distribuciones de texto, tasas de ausentismo de variables tabulares y la separabilidad de los embeddings de MRI mediante t-SNE sobre el dataset preprocesado.
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_3/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_3/DESIGN.md) e [experiments/exp_3/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_3/IMPLEMENTATION.md).
  - Implementado y ejecutado el script [experiments/exp_3/scripts/deep_eda.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_3/scripts/deep_eda.py) en el entorno `histo-DL`.
  - Calculadas las longitudes de palabra (promedio 196 para prompts clínicos y 17.8 para razonamientos) y verificado el ausentismo (0% de missingness en variables tabulares clínicas principales).
  - Graficado el balance de clases del target (56 'yes', 35 'no') y de las particiones (104 test 'NONE') en un gráfico de barras comparativo (`class_balance.png`).
  - Ejecutada la proyección 2D t-SNE de los embeddings de MRI, obteniendo un Silhouette score de -0.0129 para el split etiquetado, confirmando la necesidad matemática de fusión de modalidades.
  - Guardados los resultados en [results/deep_eda_metrics.json](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_3/results/deep_eda_metrics.json), los gráficos de distribución, balance de clase y dispersión t-SNE, y el reporte [reports/summary.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_3/reports/summary.md).
- **Next Tasks:** Diseñar e implementar el experimento `exp_4` enfocado en el diseño de validación cruzada de Monte Carlo de 100 pliegues sobre casos completos.

## [2026-08-04] - Diseño de Validación Cruzada de Monte Carlo (MCCV) con 100 Pliegues
- **Current Project State:** Se ha establecido un marco de validación cruzada robusto y sin fuga de datos sobre 190 pacientes con datos multimodales completos.
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_4/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_4/DESIGN.md) e [experiments/exp_4/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_4/IMPLEMENTATION.md).
  - Implementado y ejecutado el script [experiments/exp_4/scripts/generate_folds.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_4/scripts/generate_folds.py) en el entorno `histo-DL`.
  - Realizada la auditoría de completitud en las 195 muestras de la Tarea 1, excluyendo a 5 pacientes problemáticos que carecen de MRI embeddings o datos tabulares esenciales (`PT-pseudo_3646e0a2ae13`, `PT-pseudo_4d54f04e26ae`, `PT-pseudo_4bfd4ec864d8`, `PT-pseudo_7dbdcd6f9064`, `PT-pseudo_8636aa471ef7`).
  - Generados 100 pliegues aleatorios estratificados para los 88 pacientes etiquetados (división 70 entrenamiento, 18 validación) fijando la semilla aleatoria en `42` y aislando a los 102 casos de prueba ciega (`biopsy_decision == 'NONE'`) con el valor de pliegue `-1`.
  - Guardados los resultados del diseño en [mccv_design.csv](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_4/results/mccv_design.csv), el gráfico de estabilidad de estratificación `split_distributions.png`, y el reporte final en [summary.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_4/reports/summary.md).
- **Next Tasks:** Diseñar e implementar el experimento `exp_5` enfocado en el barrido de hiperparámetros y evaluación LOOCV del KNN Tabular.

## [2026-08-04] - Barrido de Hiperparámetros (100-splits MCCV) y Evaluación LOOCV del KNN Tabular
- **Current Project State:** Se ha optimizado y evaluado el modelo unimodal de datos tabulares utilizando la nueva metodología de validación cruzada y complete-cases.
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_5/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_5/DESIGN.md) e [experiments/exp_5/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_5/IMPLEMENTATION.md).
  - Implementado y ejecutado el script [experiments/exp_5/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_5/scripts/train.py) en el entorno `histo-DL`.
  - Configurado un pipeline de preprocesamiento que escala variables tabulares numéricas con `MinMaxScaler` y variables categóricas (`dre`) con `OneHotEncoder(handle_unknown='ignore')` de forma dinámica por pliegue para evitar fuga de datos.
  - Completado el barrido sistemático (66 combinaciones) sobre los 100 splits de Monte Carlo, identificando como configuración óptima: $k=3$, pesos `uniform` y distancia `euclidean` (promedio F1-Macro = `0.6218`).
  - Evaluada la generalización final de la configuración óptima mediante Leave-One-Out Cross-Validation (LOOCV, 88 pliegues) sobre los 88 pacientes etiquetados, obteniendo: F1-Macro de **0.6333**, Accuracy de **0.6818** (60/88), Sensibilidad de **0.8519** (46/56 TP) y Especificidad de **0.4118** (14/32 TN).
  - Guardados todos los archivos de resultados en `results/` (incluyendo predicciones y métricas JSON) y figuras de curvas de validación y matriz de confusión en `reports/`.
- **Next Tasks:** Diseñar e implementar el experimento `exp_6` enfocado en el barrido de representaciones y evaluación LOOCV de los embeddings de MRI.

## [2026-08-04] - Barrido de Representaciones (MCCV 100 splits) y Evaluación LOOCV del KNN MRI
- **Current Project State:** Se ha optimizado y evaluado el modelo unimodal de embeddings de MRI sobre 190 pacientes complete-cases.
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_6/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_6/DESIGN.md) e [experiments/exp_6/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_6/IMPLEMENTATION.md).
  - Implementado y ejecutado el script [experiments/exp_6/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_6/scripts/train.py) en el entorno `histo-DL`.
  - Configurado un barrido de representaciones que incluye Raw (MinMax), PCA (90% varianza), EmbedKit no supervisado (NTXentLoss, target_dim auto-resuelto), EmbedKit supervisado (TripletLoss, target_dim auto-resuelto) y Correlation Pruning supervisado (greedily seleccionando variables con $|r| \le \theta$, sweeps $\theta \in [0.70, 0.80, 0.90, 0.95]$).
  - Registrado que la dimensión latente intrínseca óptima resuelta dinámicamente por EmbedKit (TwoNN) fue exactamente **384** en el 100% de los splits de MCCV.
  - Identificada como la mejor configuración de representación el método **EmbedKit Supervised** con $k=3$, pesos `uniform` y distancia `euclidean` (promedio F1-Macro = `0.5469` en 100 splits).
  - Evaluada la generalización final congelando el target_dim=384 bajo Leave-One-Out Cross-Validation (LOOCV, 88 pliegues) sobre los 88 pacientes etiquetados, obteniendo: F1-Macro de **0.5335**, Accuracy de **0.5682** (50/88), Sensibilidad de **0.6852** (37/56 TP) y Especificidad de **0.3824** (13/32 TN).
  - Guardados los resultados (predicciones, métricas JSON, curvas de validación y matriz de confusión) en `results/` y `reports/`.
- **Next Tasks:** Diseñar e implementar el experimento `exp_7` enfocado en el modelo unimodal de texto (prompts clínicos) utilizando TF-IDF y barridos de hiperparámetros bajo el nuevo diseño experimental.

## [2026-08-04] - Barrido de Vocabulario y Representaciones (MCCV 100 splits) y Evaluación LOOCV del KNN Texto (TF-IDF + spaCy Lemmatization)
- **Current Project State:** Se ha optimizado y evaluado el modelo unimodal de texto (prompts clínicos) incorporando preprocesamiento avanzado con spaCy (`en_core_web_sm`).
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_7/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_7/DESIGN.md) e [experiments/exp_7/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_7/IMPLEMENTATION.md).
  - Implementado pipeline de preprocesamiento de texto con spaCy (`en_core_web_sm`): minúsculas, eliminación de caracteres especiales (`token.is_alpha`), eliminación de palabras vacías (`token.is_stop`) y lematización morfológica (`token.lemma_`).
  - Ejecutado el script [experiments/exp_7/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_7/scripts/train.py) en el entorno `histo-DL`.
  - Configurado un barrido de hiperparámetros de 5 tamaños de vocabulario (`max_features` $\in \{100, 300, 500, 1000, \text{None}\}$) e ingeniería de representaciones (Raw TF-IDF L2, PCA 90% varianza, EmbedKit no supervisado y supervisado, y Correlation Pruning $\theta \in [0.70, 0.80, 0.90, 0.95]$).
  - Identificada como la mejor configuración el vocabulario `max_features=500` con reducción **PCA** (90% varianza), $k=1$, pesos `uniform` y métrica `cosine` (promedio F1-Macro = `0.6329` en 100 splits).
  - Evaluada la generalización final bajo Leave-One-Out Cross-Validation (LOOCV, 88 pliegues) sobre los 88 pacientes etiquetados, obteniendo un aumento dramático en desempeño: F1-Macro de **0.6988** (incremento de +0.1034), Accuracy de **0.7159** (63/88), Sensibilidad de **0.7778** (42/56 TP) y Especificidad de **0.6176** (21/32 TN, aumento del 41.2% al 61.8%).
  - Guardados los resultados (predicciones, métricas JSON, curvas de validación y matriz de confusión) en `results/` y `reports/`.
- **Next Tasks:** Diseñar e implementar el experimento `exp_8` enfocado en la Fusión Tardía Multimodal (Soft Voting) evaluada con LOOCV.

## [2026-08-04] - Fusión Tardía Multimodal (Soft Voting Ensemble) y Evaluación LOOCV
- **Current Project State:** Se ha diseñado, implementado y evaluado el modelo de Fusión Tardía (*Soft Voting*) integrando las distribuciones de probabilidad out-of-fold de los tres modelos unimodales óptimos.
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_8/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_8/DESIGN.md) e [experiments/exp_8/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_8/IMPLEMENTATION.md).
  - Ejecutado el script [experiments/exp_8/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_8/scripts/train.py) en el entorno `histo-DL`.
  - Generadas las distribuciones de probabilidad out-of-fold en LOOCV para cada paciente utilizando los 3 modelos unimodales óptimos (Tabular `exp_5`, MRI Embeddings con EmbedKit supervisado `exp_6`, y Texto con spaCy + TF-IDF + PCA `exp_7`).
  - Evaluadas las estrategias de fusión: Trimodal de Pesos Iguales, Trimodal Ponderado por Rejilla, y las tres ablaciones bimodales (Tabular+Texto, Tabular+MRI, Texto+MRI).
  - Demostrado empíricamente que la **Fusión Tardía Trimodal (*Soft Voting*) SUPERA a todos los modelos unimodales e integraciones bimodales**, alcanzando un Macro-F1 de **0.7171** (frente a 0.6988 del mejor unimodal), una Accuracy de **0.7500** (66/88 aciertos), una Sensibilidad de **0.8889** (48/54 TP) y un **AUROC de 0.7821**.
  - Guardados los resultados (predicciones LOOCV, métricas JSON, curvas ROC y matriz de confusión) en `results/` y `reports/`.
- **Next Tasks:** Diseñar e implementar el experimento `exp_9` enfocado en la predicción LOOCV de confiabilidad diagnóstica e importancia de variables clínicas (`clinical_reasoning.csv`).

## [2026-08-04] - Predicción Out-of-Fold de Confiabilidad Diagnóstica mediante ICI y Meta-Umbrales (exp_9)
- **Current Project State:** Se ha diseñado, implementado y evaluado el modelo de predicción de confiabilidad médica (`confidence`) desacoplando el aprendizaje de umbrales en MCCV de la prueba final congelada en LOOCV.
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_9/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_9/DESIGN.md) e [experiments/exp_9/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_9/IMPLEMENTATION.md).
  - Ejecutado el script [experiments/exp_9/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_9/scripts/train.py) en el entorno `histo-DL`.
  - **Fase A (Aprendizaje de Meta-Umbrales en 100 Splits MCCV):** Ajustados árboles de decisión 1D sobre el Índice Compuesto $ICI$, calculando los meta-umbrales promedio congelados: $\bar{\tau}_1 = 0.0743$ (desviación estándar $0.0969$) y $\bar{\tau}_2 = 0.3321$ (desviación estándar $0.2261$). Almacenados en `results/meta_thresholds.json`.
  - **Fase B (Evaluación Congelada LOOCV 88 Pliegues):** Clasificada la confiabilidad médica out-of-fold a ciegas usando los meta-umbrales congelados $(\bar{\tau}_1, \bar{\tau}_2)$ sin re-entrenar árboles en LOOCV. Obtenidas las métricas: $F1$-Macro multiclase de **0.3691**, Accuracy del **39.8%** (35/88 aciertos) y correlación de Spearman $\rho = 0.1228$.
  - Guardados los resultados (predicciones LOOCV, métricas JSON, histograma de umbrales y matriz de confusión 3x3) en `results/` y `reports/`.
- **Next Tasks:** Diseñar e implementar el experimento `exp_10` enfocado en el entrenamiento balanceado de los meta-umbrales de confiabilidad diagnóstica.

## [2026-08-05] - Predicción Out-of-Fold de Confiabilidad Diagnóstica con Entrenamiento Balanceado (exp_10)
- **Current Project State:** Se ha diseñado, implementado y evaluado el experimento de predicción de confiabilidad médica (`confidence`) incorporando pesos inversamente proporcionales (`class_weight='balanced'`) en la Fase A (MCCV).
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_10/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_10/DESIGN.md) e [experiments/exp_10/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_10/IMPLEMENTATION.md).
  - Ejecutado el script [experiments/exp_10/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_10/scripts/train.py) en el entorno `histo-DL`.
  - **Fase A (Aprendizaje de Meta-Umbrales Balanceados en 100 Splits MCCV):** Se ajustaron árboles de decisión 1D con `class_weight='balanced'` sobre el Índice Compuesto $ICI$, reduciendo y desplazando los meta-umbrales promedio congelados: $\bar{\tau}_1 = 0.0669$ (desviación estándar $0.0769$) y $\bar{\tau}_2 = 0.2960$ (desviación estándar $0.2023$). Almacenados en `results/meta_thresholds.json`.
  - **Fase B (Evaluación Congelada LOOCV 88 Pliegues):** Clasificada la confiabilidad médica out-of-fold a ciegas usando los meta-umbrales congelados $(\bar{\tau}_1, \bar{\tau}_2)$ sin re-entrenar árboles en LOOCV. Obtenidas las métricas: $F1$-Macro multiclase de **0.3691**, Accuracy del **39.8%** (35/88 aciertos) y correlación de Spearman $\rho = 0.1228$.
  - Guardados los resultados (predicciones LOOCV, métricas JSON, histograma de umbrales y matriz de confusión 3x3) en `results/` y `reports/`.
- **Next Tasks:** Diseñar e implementar el experimento `exp_11` enfocado en el umbralizado dinámico local dentro de cada pliegue de LOOCV para contrastar la hipótesis de difuminación.

## [2026-08-05] - Evaluacion de Umbrales Dinámicos Locales LOOCV para Predicción de Confiabilidad (exp_11)
- **Current Project State:** Se ha diseñado, implementado y evaluado el experimento de umbralizado dinámico local dentro de cada pliegue de LOOCV sin pasar por la meta-agregación de Monte Carlo.
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_11/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_11/DESIGN.md) e [experiments/exp_11/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_11/IMPLEMENTATION.md).
  - Ejecutado el script [experiments/exp_11/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_11/scripts/train.py) en el entorno `histo-DL`.
  - **Resultados de Umbrales Dinámicos LOOCV:** La media de los umbrales dinámicos fue $\bar{\tau}_1 = 0.0966$ ($\text{std} = 0.0000$) y $\bar{\tau}_2 = 0.2892$ ($\text{std} = 0.1202$). El desempeño out-of-fold alcanzó un $F1$-Macro multiclase de **0.3388** y una Accuracy del **36.36%** (32/88 aciertos).
  - **Conclusión de la Prueba de Hipótesis:** El meta-promedio de Monte Carlo (`exp_10` con $F1 = 0.3691$) supera al umbralizado dinámico local (`exp_11` con $F1 = 0.3388$), demostrando que la meta-agregación actúa como un ensamble (*Bagging of Decision Cut-Points*) que reduce el ruido muestral local en conjuntos pequeños.
  - Guardados los resultados (predicciones LOOCV, métricas JSON, gráfico de evolución de umbrales y matriz de confusión 3x3) en `results/` y `reports/`.
- **Next Tasks:** Diseñar e implementar el experimento `exp_12` enfocado en la clasificación de confiabilidad diagnóstica basada en el vector de probabilidades 3D $\mathbf{p} = [p_{\text{tab}}, p_{\text{mri}}, p_{\text{text}}]$.

## [2026-08-05] - Predicción Out-of-Fold de Confiabilidad mediante Vector de Probabilidades 3D (exp_12)
- **Current Project State:** Se ha diseñado, implementado y evaluado el experimento de predicción de confiabilidad diagnóstica reemplazando el escalar $ICI$ por la matriz completa de probabilidades 3D $\mathbf{p} = [p_{\text{tab}}, p_{\text{mri}}, p_{\text{text}}]^T$ con un Árbol de Decisión multidimensional (`class_weight='balanced'`) en LOOCV (88 pliegues).
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_12/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_12/DESIGN.md) e [experiments/exp_12/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_12/IMPLEMENTATION.md).
  - Ejecutado el script [experiments/exp_12/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_12/scripts/train.py) en el entorno `histo-DL`.
  - **Importancia de Características Modales Aprendida:** El árbol descubrió automáticamente que la probabilidad tabular $p_{\text{tab}}$ ostenta el **61.8%** del poder discriminativo (`importance = 0.6184`), seguida por la MRI $p_{\text{mri}}$ con el **33.5%** (`importance = 0.3347`), mientras que el texto prompt $p_{\text{text}}$ aporta únicamente el **4.7%** (`importance = 0.0469`).
  - **Resultados de Desempeño:** Alcanzado un $F1$-Macro multiclase de **0.3331** y una Accuracy del **37.50%** (33/88 aciertos).
  - **Conclusión de Investigación:** Las probabilidades aisladas de los clasificadores KNN ($p_{\text{tab}}, p_{\text{mri}}, p_{\text{text}}$) no son suficientes por sí solas para mapear la confiabilidad médica, demostrando que la incertidumbre real proviene de omisiones de historial clínico anotadas en `reasoning_text`.
  - Guardados los resultados (predicciones LOOCV, métricas JSON, gráfico de importancias y matriz de confusión 3x3) en `results/` y `reports/`.
- **Next Tasks:** Diseñar e implementar el experimento de atribución de relevancia de las 10 variables clínicas (`weight_*`) mediante perturbación de moda.

## [2026-08-05] - Creación de Configuración .gitignore para Control de Versiones
- **Current Project State:** Se ha configurado el archivo de exclusiones de Git en la raíz del repositorio.
- **Specific Advancements:**
  - Creado el archivo [.gitignore](file:///home/jmalagont/project/pathology-reasoning/.gitignore).
  - Configuradas las exclusiones explícitas para archivos de variables de entorno (`.env`, `.env.*`), el directorio de datos (`data/`), artefactos de caché de Python, serializaciones pesadas (`*.pkl`, `*.pth`, `*.joblib`), checkpoints de Jupyter y configuraciones del IDE.
- **Next Tasks:** Diseñar e implementar el experimento `exp_13` enfocado en el barrido de hiperparámetros y evaluación LOOCV de Fuzzy KNN sobre datos tabulares.

## [2026-08-05] - Evaluacion Out-of-Fold de Tabular Fuzzy KNN con Soft Targets (exp_13)
- **Current Project State:** Se ha diseñado, implementado y evaluado el barrido de hiperparámetros (100 MCCV splits) y la evaluación LOOCV del clasificador Fuzzy KNN (`KNeighborsRegressor`) con etiquetas suaves atenuadas por confiabilidad médica ($\tilde{y} \in [0.0, 1.0]$).
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_13/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_13/DESIGN.md) e [experiments/exp_13/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_13/IMPLEMENTATION.md).
  - Ejecutado el script [experiments/exp_13/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_13/scripts/train.py) en el entorno `histo-DL`.
  - **Fase A (Barrido MCCV en 100 Splits):** Los mejores hiperparámetros fueron $k^*=1$, `uniform`, `euclidean` con un $F1$-Macro promedio de **0.6117**.
  - **Fase B (Evaluación Out-of-Fold LOOCV 88 Pliegues):** Alcanzado un $F1$-Macro de **0.6364** (superando al baseline `exp_5` de **0.6333**), una Accuracy del **65.91%** y un **salto significativo en Especificidad del 41.18% al 52.94% (+11.76%)**.
  - **Conclusión Metodológica:** Fuzzy KNN redujo exitosamente el sesgo dogmático de la clase mayoritaria (biopsia positiva), mejorando la detección de casos negativos sin biopsia.
  - Guardados los resultados (predicciones LOOCV, métricas JSON, gráfico de matriz de confusión y curva ROC) en `results/` y `reports/`.
- **Next Tasks:** Diseñar e implementar el experimento `exp_14` enfocado en el barrido de representaciones (Raw, PCA, EmbedKit) y evaluación LOOCV de Fuzzy KNN sobre datos de MRI.

## [2026-08-05] - Evaluacion Out-of-Fold de MRI Fuzzy KNN con Soft Targets (exp_14)
- **Current Project State:** Se ha diseñado, implementado y evaluado el barrido de representaciones y parámetros (100 MCCV splits) y la evaluación LOOCV de Fuzzy KNN sobre embeddings de MRI de 1024 dimensiones con etiquetas suaves atenuadas por confiabilidad médica ($\tilde{y} \in [0.0, 1.0]$).
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_14/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_14/DESIGN.md) e [experiments/exp_14/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_14/IMPLEMENTATION.md).
  - Ejecutado el script [experiments/exp_14/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_14/scripts/train.py) en el entorno `histo-DL`.
  - **Fase A (Barrido MCCV de Representaciones en 100 Splits):** La mejor configuración fue `embedkit_unsup` con $k^*=3$, `uniform`, `euclidean` y un $F1$-Macro promedio de **0.5422**.
  - **Fase B (Evaluación Out-of-Fold LOOCV 88 Pliegues):** Alcanzado un $F1$-Macro de **0.5335**, una Accuracy del **56.82%**, Sensibilidad del **68.52%**, Especificidad del **38.24%** y AUROC de **0.5387**.
  - **Conclusión Metodológica:** En el espacio de embeddings de alta dimensión de MRI (1024D proyectado a 384D), la topología de vecinos más cercanos preserva las fronteras discretas del `exp_6`, mientras que Fuzzy KNN proporciona probabilidades suaves continuas verdaderamente calibradas con un Brier score de **0.2623**.
  - Guardados los resultados (predicciones LOOCV, métricas JSON, gráfico de matriz de confusión y curva ROC) en `results/` y `reports/`.
- **Next Tasks:** Diseñar e implementar el experimento `exp_15` enfocado en el barrido de vocabulario (`max_features`), representaciones (Raw, PCA, EmbedKit) y evaluación LOOCV de Fuzzy KNN sobre textos clínicos TF-IDF.

## [2026-08-05] - Evaluacion Out-of-Fold de Text TF-IDF Fuzzy KNN con Soft Targets (exp_15)
- **Current Project State:** Se ha diseñado, implementado y evaluado el barrido masivo de vocabulario y representaciones (640 configuraciones en 100 MCCV splits) y la evaluación LOOCV de Fuzzy KNN sobre características TF-IDF de texto clínico con etiquetas suaves atenuadas por confiabilidad médica ($\tilde{y} \in [0.0, 1.0]$).
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_15/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_15/DESIGN.md) e [experiments/exp_15/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_15/IMPLEMENTATION.md).
  - Ejecutado el script [experiments/exp_15/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_15/scripts/train.py) en el entorno `histo-DL`.
  - **Fase A (Barrido MCCV en 100 Splits):** La mejor configuración fue `max_features=None`, `pca` con $k^*=3$, `uniform`, `cosine` y un $F1$-Macro promedio de **0.6393** (superando la Fase A del `exp_7` de **0.6329**).
  - **Fase B (Evaluación Out-of-Fold LOOCV 88 Pliegues):** Alcanzado un $F1$-Macro de **0.6558**, una Accuracy del **69.32%**, Sensibilidad del **83.33%** (+5.55% respecto a `exp_7`), Especificidad del **47.06%**, AUROC de **0.6868** y un Brier score de **0.2195** (excelente calibración de probabilidades).
  - Guardados los resultados (predicciones LOOCV, métricas JSON, gráfico de matriz de confusión y curva ROC) en `results/` y `reports/`.
- **Next Tasks:** Diseñar e implementar el experimento `exp_16` enfocado en la Fusión Tardía Trimodal Soft-Voting en LOOCV con los 3 modelos óptimos Fuzzy KNN.

## [2026-08-05] - Evaluacion Out-of-Fold de Multimodal Fuzzy KNN Late Fusion (exp_16)
- **Current Project State:** Se ha diseñado, implementado y evaluado la Fusión Tardía Trimodal Soft-Voting en LOOCV (88 pliegues) reentrenando dinámicamente los 3 modelos óptimos unimodales de Fuzzy KNN (`exp_13` Tabular, `exp_14` MRI, `exp_15` Texto).
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_16/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_16/DESIGN.md) e [experiments/exp_16/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_16/IMPLEMENTATION.md).
  - Ejecutado el script [experiments/exp_16/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_16/scripts/train.py) en el entorno `histo-DL`.
  - **Pesos Óptimos Encontrados (`Optimal-Weighted-Trimodal`):** $w_{\text{tab}} = 0.15$, $w_{\text{mri}} = 0.55$, $w_{\text{text}} = 0.30$.
  - **Desempeño LOOCV Out-of-Fold:** Alcanzado un $F1$-Macro de **0.6813**, una Accuracy del **71.59%**, Sensibilidad del **85.19%**, Especificidad del **50.00%**, AUROC de **0.7053** y un **salto masivo en la calibración Brier a 0.2093** (reducido desde 0.29 en los modelos individuales).
  - Guardados los resultados (predicciones LOOCV, métricas JSON, gráfico de matriz de confusión y curvas ROC comparativas) en `results/` y `reports/`.
- **Next Tasks:** Diseñar e implementar el experimento `exp_17` enfocado en la predicción balanceada de confiabilidad médica mediante meta-umbrales sobre $ICI_{\text{fuzzy}}$.

## [2026-08-05] - Evaluacion Out-of-Fold de Diagnostic Confidence via Class-Weighted Composite Fuzzy ICI (exp_17 - Corregido)
- **Current Project State:** Se ha corregido y re-ejecutado el experimento `exp_17` utilizando exactamente la **Ecuación Compuesta de Confiabilidad ($ICI_{\text{composite}}$)** idéntica a `exp_9`, `exp_10` y `exp_11`:
  $$ICI_{\text{composite}} = (2.0 \cdot |\bar{p} - 0.50|) \cdot (1.0 - 2.0 \cdot \sigma_p)$$
- **Specific Advancements:**
  - Corregido el script [experiments/exp_17/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_17/scripts/train.py) y ejecutado en el entorno `histo-DL`.
  - **Fase A (Aprendizaje de Meta-Umbrales Balanceados):** Obtenidos $\bar{\tau}_1 = 0.0180$ ($\text{std} = 0.0318$) y $\bar{\tau}_2 = 0.1266$ ($\text{std} = 0.0612$).
  - **Fase B (Evaluación Out-of-Fold LOOCV 88 Pliegues):** **RÉCORD HISTÓRICO** alcanzando un $F1$-Macro de 3 clases de **`0.4470`** (superando holgadamente a `exp_10` de **0.3691**), una Accuracy del **`57.95%`** (51/88 aciertos, superando el **39.77%** de `exp_10`) y un coeficiente de Spearman **$\rho = 0.2790$ con significancia estadística ($p = 0.008487 < 0.01$)**.
  - **Conclusión Metodológica Clave:** Al integrar la probabilidad suave continua de Fuzzy KNN dentro de la fórmula compuesta del $ICI$ (que pondera tanto la distancia a la frontera $0.50$ como la desviación estándar inter-modal $\sigma_p$), la predicción de la incertidumbre médica anotada por el patólogo alcanza su máximo desempeño histórico con correlación de rangos estadísticamente significativa.
  - Guardados los resultados (predicciones LOOCV, métricas JSON, gráfico de histograma de umbrales y matriz de confusión 3x3) en `results/` y `reports/`.
- **Next Tasks:** Diseñar e implementar el experimento `exp_18` enfocado en la Fusión Tardía Híbrida Soft-Voting en LOOCV combining Tabular Fuzzy KNN + MRI Hard KNN + Text Hard KNN.

## [2026-08-05] - Evaluacion Out-of-Fold de Hybrid Multimodal Late Fusion (exp_18)
- **Current Project State:** Se ha diseñado, implementado y evaluado la Fusión Tardía Híbrida en LOOCV (88 pliegues) combinando la probabilidad suave continua de **Tabular Fuzzy KNN (`exp_13`)** con las probabilidades duras de **MRI Hard KNN (`exp_6`)** y **Texto Hard KNN (`exp_7`)**.
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_18/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_18/DESIGN.md) e [experiments/exp_18/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_18/IMPLEMENTATION.md).
  - Ejecutado el script [experiments/exp_18/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_18/scripts/train.py) en el entorno `histo-DL`.
  - **Pesos Híbridos Óptimos Encontrados (`Optimal-Weighted-Hybrid`):** $w_{\text{tab\_fuzzy}} = 0.05$, $w_{\text{mri\_hard}} = 0.50$, $w_{\text{text\_hard}} = 0.45$.
  - **Desempeño LOOCV Out-of-Fold:** Alcanzado un $F1$-Macro de **0.6713**, una Accuracy del **70.45%** (62/88 aciertos), Sensibilidad del **83.33%**, Especificidad del **50.00%**, AUROC de **0.7334** y Brier score de **0.2151** (y en Fusión Igual Brier = **0.1997**).
  - Guardados los resultados (predicciones LOOCV, métricas JSON, gráfico de matriz de confusión y curvas ROC comparativas) en `results/` y `reports/`.
- **Next Tasks:** Diseñar e implementar el experimento `exp_19` enfocado en la predicción balanceada de confiabilidad médica mediante meta-umbrales sobre $ICI_{\text{hybrid}}$.

## [2026-08-05] - Evaluacion Out-of-Fold de Diagnostic Confidence via Class-Weighted Hybrid Composite ICI (exp_19)
- **Current Project State:** Se ha diseñado, implementado y evaluado el aprendizaje de meta-umbrales $(\bar{\tau}_1, \bar{\tau}_2)$ en 100 splits MCCV mediante Árboles de Decisión 1D (`class_weight='balanced'`) sobre el Índice de Conflicto Compuesto Híbrido ($ICI_{\text{hybrid}}$) calculado a partir de las probabilidades del ensamble híbrido (`exp_18` Tabular Fuzzy, MRI Hard, Texto Hard).
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_19/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_19/DESIGN.md) e [experiments/exp_19/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_19/IMPLEMENTATION.md).
  - Aplicada la ecuación compuesta del $ICI$:
    $$ICI_{\text{hybrid}, i} = (2.0 \cdot |\bar{p}_i - 0.50|) \cdot (1.0 - 2.0 \cdot \sigma_{p, i})$$
  - Ejecutado el script [experiments/exp_19/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_19/scripts/train.py) en el entorno `histo-DL`.
  - **Fase A (Aprendizaje de Meta-Umbrales Balanceados):** Obtenidos $\bar{\tau}_1 = 0.1169$ ($\text{std} = 0.1629$) y $\bar{\tau}_2 = 0.4338$ ($\text{std} = 0.2055$).
  - **Fase B (Evaluación Out-of-Fold LOOCV 88 Pliegues):** Alcanzado un $F1$-Macro de 3 clases de **0.3885**, una Accuracy del **42.05%** (37/88 aciertos) y Spearman $\rho = 0.1681$ ($p = 0.117$, no significativo).
  - **Conclusión Metodológica Clave:** El ensamble completamente suave Fuzzy (`exp_17`, $F1 = 0.4470$, $\rho = 0.2790$, $p < 0.01$) supera al ensamble híbrido (`exp_19`) y al rígido (`exp_10`). Esto se debe a que la continuidad probabilística de las 3 modalidades en `exp_17` elimina discontinuidades por escalones de voto, permitiendo que el árbol de decisión aprenda meta-umbrales estables e informativos.
  - Guardados los resultados (predicciones LOOCV, métricas JSON, gráfico de histograma de umbrales y matriz de confusión 3x3) en `results/` y `reports/`.

## [2026-08-05] - Evaluacion Out-of-Fold de Atribucion de Relevancia Clinica via Perturbacion de Moda/Mediana (exp_20)
- **Current Project State:** Se ha diseñado, implementado y evaluado la atribución de relevancia clínica de las 8 variables tabulares mediante perturbación por moda/mediana en la cohorte $N=88$ bajo el esquema de Dos Fases (MCCV 100 splits $\to$ LOOCV 88 pliegues) sobre el modelo canónico Tabular Fuzzy KNN (`exp_13`).
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental y de implementación en [experiments/exp_20/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_20/DESIGN.md) e [experiments/exp_20/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_20/IMPLEMENTATION.md).
  - **Fase A (Aprendizaje de Umbrales Independientes):** Entrenados 8 Árboles de Decisión 1D independientes (`class_weight='balanced'`) en 100 splits MCCV para obtener los meta-umbrales $(\bar{\tau}_1^{(j)}, \bar{\tau}_2^{(j)}, \bar{\tau}_3^{(j)})$ específicos de cada variable.
  - **Fase B (Evaluación Out-of-Fold LOOCV 88 Pliegues):** Evaluado el grado de coincidencia mediante la correlación de Spearman ($\rho$) contra las anotaciones reales de los urologistas (`weight_*` en `clinical_reasoning.csv`).
    1. **Examen Físico Tacto Rectal (`dre`):** Alcanzó la correlación positiva más alta y estadísticamente más significativa del experimento: **$\rho = 0.4290$ ($p = 3.04 \times 10^{-5} < 0.0001$)**.
    2. **Cinética del PSA (`psav`):** Alcanzó una correlación positiva estadísticamente significativa de **$\rho = 0.2617$ ($p = 0.0138 < 0.05$)**.
    3. **Variables Secundarias:** `age` ($\rho = 0.1965, p = 0.066$) y `psa` ($\rho = 0.1840, p = 0.086$) mostraron tendencias de correlación positiva.
  - Guardados los resultados (desplazamientos LOOCV, métricas JSON, gráfico de barras comparativo y matrices de confusión 4x4) en `results/` y `reports/`.
- **Next Tasks:** Ejecutar la evaluación de atribución de relevancia clínica mediante SHAP (`exp_21`).

## [2026-08-05] - Evaluacion de Atribucion de Relevancia Clinica via Valores de Shapley SHAP (exp_21)
- **Current Project State:** Se ha diseñado, implementado y evaluado la atribución de relevancia clínica a nivel de muestra mediante valores de Shapley SHAP (`KernelExplainer`) sobre el modelo Tabular Fuzzy KNN (`exp_13`) bajo la arquitectura de Dos Fases (100 MCCV splits $\to$ LOOCV 88 pliegues).
- **Specific Advancements:**
  - Diseñado y aprobado el plan experimental en [experiments/exp_21/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_21/DESIGN.md) e [experiments/exp_21/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_21/IMPLEMENTATION.md).
  - Ejecutado el script [experiments/exp_21/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_21/scripts/train.py) en el entorno `histo-DL`.
  - **Fase A (Aprendizaje de Meta-Umbrales Balanceados):** Entrenados Árboles de Decisión 1D (`class_weight='balanced'`) en 100 splits MCCV para aprender los meta-umbrales $(\bar{\tau}_1^{(j)}, \bar{\tau}_2^{(j)}, \bar{\tau}_3^{(j)})$ específicos de cada variable.
  - **Fase B (Evaluación Out-of-Fold LOOCV 88 Pliegues):**
    1. **Resolución de Colinealidad en PSA (`psa`):** SHAP resolvió la colinealidad con `psad` y `vol`, alcanzando una correlación de Spearman **$\rho = 0.3593$ ($p = 0.000586 < 0.001$, Altamente Significativa)**, aumentando el $F1$-Macro a **0.1728** y la Accuracy al **26.14%**.
    2. **Mejora Consistente del $F1$-Macro de Clasificación:** El $F1$-Macro aumentó en todas las variables respecto a la perturbación (`exp_20`): `psad` ($0.2182$), `dre` ($0.2032$), `pirads` ($0.1987$), `vol` ($0.1906$), `psa` ($0.1728$).
  - Generadas y guardadas las matrices de confusión 4x4 individuales, el grid consolidado 2x3, los histogramas de $F1$-Macro y $|\rho|$, y las métricas JSON en `results/` y `reports/figures/`.
- **Next Tasks:** Ejecutar la atribución de relevancia clínica con árboles multivariados SHAP 6D (`exp_22`).

## [2026-08-06] - Evaluacion de Atribucion con Arboles de Decision Multivariados sobre Vectores SHAP 6D (exp_22)
- **Current Project State:** Se ha entrenado un árbol de decisión multivariado (`DecisionTreeClassifier(max_depth=3, class_weight='balanced')`) para cada variable objetivo $j$, alimentado con el **vector completo de 6 dimensiones de atribuciones SHAP** $\boldsymbol{\Phi}^{(i)} \in \mathbb{R}^6$ en LOOCV ($N=88$).
- **Specific Advancements:**
  - Diseñado, aprobado y ejecutado el experimento en [experiments/exp_22/DESIGN.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_22/DESIGN.md), [experiments/exp_22/IMPLEMENTATION.md](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_22/IMPLEMENTATION.md) y [experiments/exp_22/scripts/train.py](file:///home/jmalagont/project/pathology-reasoning/experiments/exp_22/scripts/train.py).
  - **Gran Avance en $F1$-Macro y Superación Demostrada de Baselines de Azar:**
    1. **`pirads` (PI-RADS):** El $F1$-Macro aumentó drásticamente a **`0.3991`** (Superando el Azar Estratificado de $0.3319$ y $exp\_21$ de $0.1987$ en +0.2004), con una Accuracy del **`61.36%`** y Correlación de Spearman **$\rho = 0.3019$ ($p = 0.00425 < 0.01$, Estadísticamente Significativa)**.
    2. **`psa` (PSA Sérico):** El $F1$-Macro aumentó a **`0.3446`** (Superando el Azar Estratificado de $0.2481$ y $exp\_21$ de $0.1728$ en +0.1718), con una Accuracy del **`45.45%`** y Correlación de Spearman **$\rho = 0.3738$ ($p = 0.000333 < 0.001$, Altamente Significativa)**.
    3. **`age` (Edad):** El $F1$-Macro aumentó a **`0.2609`** (Superando el Azar Estratificado de $0.2477$).
  - Guardadas las matrices de confusión 4x4 independientes, los histogramas comparativos y métricas JSON en `results/` y `reports/figures/`.
- **Next Tasks:** Generar la síntesis del texto de razonamiento clínico en lenguaje natural (Discussion / Task 1C).

## [2026-08-18] - Kernel Density Matrix (KDM) como Generalizacion Diferenciable del Fuzzy KNN Tabular (exp_23)
- **Current Project State:** Se reemplazó la memoria congelada del Fuzzy KNN tabular (`exp_13`, k=1, uniform,
  euclidean) por un `KDMClassModel` (`n_comp = n_train`) cuyos prototipos, pesos de mezcla y ancho de banda
  del kernel RBF son diferenciables y se entrenan end-to-end, sobre el mismo cohorte de N=88 pacientes,
  idéntico espacio de características de 12 dimensiones y el mismo arnés MCCV (100 splits)/LOOCV que
  `exp_13`.
- **Specific Advancements:**
  - Diseñado, aprobado y ejecutado el experimento en `experiments/exp_23/DESIGN.md`,
    `experiments/exp_23/IMPLEMENTATION.md` y `experiments/exp_23/scripts/train.py`.
  - Se recompensó inline la referencia Fuzzy KNN (`k=1, uniform, euclidean`) sobre las mismas filas/splits
    para garantizar comparabilidad byte-a-byte, reproduciendo el Macro-F1 publicado de `exp_13`
    (**`0.6364`**, confusión `tp=40, tn=18, fp=16, fn=14`), corrigiendo además el conteo de clases erróneo
    56/32 de `exp_13` por el correcto **54 yes / 34 no**.
  - **Brazo B (soft targets, `sigma_mult=2.0, encoder=identity`) superó a la referencia Fuzzy KNN en
    Macro-F1 medio:** **`0.6694`** vs. `0.6364` de la referencia (Accuracy 71.59% vs. 65.91%, AUROC 0.6498
    vs. 0.6304, Brier 0.2263 vs. 0.2908). El brazo A (hard, `encoder=linear`) obtuvo **`0.5636`**, por
    debajo de la referencia.
  - **La prueba de McNemar no alcanzó significancia en ninguno de los dos brazos** (brazo duro: b=7, c=10,
    p=0.6291; brazo suave: b=11, c=6, p=0.3323) — la ganancia en Macro-F1 medio del brazo suave no se
    tradujo en una diferencia estadísticamente significativa de las decisiones binarias LOOCV frente a la
    referencia, por lo que H1 se marcó como **verificación parcial**, no éxito completo.
  - Como objetivo secundario, ninguna de las señales nativas de incertidumbre del KDM (`entropy`,
    `log_marginal`, ambos brazos) superó la línea base de `exp_17` (Composite Fuzzy ICI, Macro-F1 0.4470)
    para predecir la confianza diagnóstica de 3 clases del urólogo; la mejor señal no informada por el
    objetivo fue `log_marginal_hard` con Macro-F1 **`0.3340`**.
- **Next Tasks:** Investigar si la estructura interna del conjunto de partículas del KDM (más allá de la
  media colapsada `entropy`/`log_marginal`) contiene señal adicional de incertidumbre no explotada
  (`exp_24`).

## [2026-08-18] - Descomposicion de Incertidumbre desde el Conjunto de Particulas del KDM (exp_24)
- **Current Project State:** Se extrajo la matriz de densidad de salida del KDM (`exp_23`) como lo que
  estructuralmente es — un conjunto ponderado de partículas `{(w_j(x), p_j)}` que `dm2discrete` colapsa a
  su media antes de devolver la probabilidad de clase — y se investigó si la estructura interna de ese
  conjunto (no solo su media colapsada) predice mejor la confianza diagnóstica de 3 clases del urólogo.
- **Specific Advancements:**
  - Diseñado, aprobado y ejecutado el experimento en `experiments/exp_24/DESIGN.md`,
    `experiments/exp_24/IMPLEMENTATION.md` y `experiments/exp_24/scripts/train.py`.
  - **Hallazgo estructural confirmado numéricamente (H3):** un `c_y` inicializado one-hot (el brazo duro de
    `exp_23`) es un punto fijo exacto del gradiente de la regla de Born — verificado analíticamente, contra
    la propia rejilla de `exp_23` (16/16 pares `y_train=True` vs. `False` idénticos byte-a-byte en el brazo
    duro, 0/16 en el suave) y numéricamente en este experimento (`max_cy_drift_eps0 = 0.0` exacto tras 300
    épocas con `y_train=True`). Esto hace que la descomposición aleatoria/epistémica sea trivialmente
    degenerada (`h_aleatoric ≡ 0`, `h_epistemic ≡ h_total`) en el brazo duro de `exp_23`.
  - Se añadió un tercer brazo (**Arm C**, `c_y` con suavizado ε de etiqueta, `y_train=True`, pérdida dura
    sobre el resultado binario real, sin usar la anotación `confidence` en ningún punto) que sí escapa del
    punto fijo (deriva máxima de `c_y` de `0.315` tras el entrenamiento, frente a `0.0` exacto en `eps=0`).
  - **H1 (parcialmente confirmada):** la mejor cabeza no informada por el objetivo — un ensamble
    multivariado de árboles (profundidad 3, votación out-of-fold) sobre las 7 señales de partículas del
    brazo A — alcanzó Macro-F1 **`0.4368`**, superando ampliamente el mejor resultado de `exp_23`
    (`0.3340`, +0.1028) pero sin alcanzar la línea base de `exp_17` (`0.4470`, -0.0102).
  - **H2 (refutada):** en el brazo C, único brazo donde `h_aleatoric` es no degenerado y no informado por
    el objetivo, la correlación de Spearman con `confidence` no fue significativa (ρ=0.1321, p=0.2199), y
    el ensamble multivariado completo (7 señales) rindió **peor** que su propia ablación de 2 señales
    (`0.3699` vs. `0.3947`) — la descomposición aleatoria/epistémica no aportó la señal esperada.
  - **Hallazgo no anticipado:** la ganancia real del brazo A (0.4368 vs. ablación 0.3600) proviene
    enteramente de las señales de geometría de pesos de las partículas (`h_weights`, `log_ess`, `w_max`),
    que no dependen de `c_y` y por tanto son inmunes a la degeneración de H3 — no de la descomposición
    aleatoria/epistémica que motivó el diseño del experimento.
  - Ambos brazos A y B reprodujeron exactamente (16 dígitos) el Macro-F1 LOOCV publicado de `exp_23`
    (`0.5636363636363637` y `0.6694214876033058` respectivamente), confirmando que la reutilización
    verbatim del pipeline de `exp_23` fue fiel.
- **Next Tasks:** Aislar qué subconjunto de señales de geometría de pesos impulsa realmente el resultado
  del brazo A (importancias de features de los árboles ajustados) antes de tratarlo como un hallazgo
  confiable; considerar mecanismos alternativos de desanclaje del punto fijo (p. ej. temperatura por
  partícula aprendida) si se retoma la vía de descomposición aleatoria/epistémica.

## [2026-08-18] - Extracción de `src/methods` y `src/evaluation`: KDM Multimodal Basado en Memoria (mem_kdm)
- **Current Project State:** Todo experimento hasta `exp_24` es un `train.py` autocontenido de 230-960
  líneas, sin imports entre experimentos. `exp_23` y `exp_24` comparten ~476 líneas idénticas (~50% de
  ambos scripts: `load_cohort`, `build_features_fixed_categories`, `build_kdm`, `init_and_check`,
  `train_kdm`, `mcnemar_exact`, la tríada de cabezas de confianza 1D). El próximo método —un KDM basado en
  memoria que combine tabular, MRI y texto, la generalización multimodal de `exp_23`/`exp_24`— habría sido
  una tercera copia. Se extrajo primero la capa compartida antes de implementar el método.
- **Specific Advancements:**
  - Creados `src/methods/base.py` (protocolo `Method`, dataclass `Targets`, funciones de umbral-meta
    compartidas `fit_meta_thresholds[_safe]`/`apply_meta_thresholds`/`fit_predict_heldout_trees`, portadas
    de `exp_23:506-587` y `exp_24:543-599`) y `src/methods/mem_kdm.py` (~600 líneas: el método completo).
  - Creados `src/evaluation/{data,protocol,metrics,reporting}.py`: `load_cohort`/`resolve_data_dir`
    (portados de `exp_23:47-126`, con la aserción `N=88`/`54 sí`/`34 no` que corrige el 56/32 publicado por
    `exp_13`), el harness MCCV/LOOCV de dos fases con una regla explícita de desempate de configuraciones
    (`select_best`, ausente en `exp_13`-`24`), y las métricas (`binary_metrics`, `mcnemar_exact`,
    `confidence_metrics`) deduplicadas de sus copias byte-idénticas.
  - **`MemKDM` es una única clase para 1..N modalidades**, no dos clases separadas: con un bloque, el
    kernel del bloque se usa directamente (sin envoltura `CrossProductKernelLayer`); con N bloques, kernel
    producto `k(x,c) = Π_m RBF_m(x_m, c_m)` vía `CrossProductKernelLayer` anidado a la derecha. Cada
    modalidad tiene su propio ancho de banda σ. `MemKDM.from_unimodal(models, freeze_kernels=True)`
    transfiere los σ ajustados unimodalmente al modelo conjunto, evitando una rejilla combinatoria conjunta.
  - **La supervisión es blanda en todo momento**; "duro" es el caso especial `y_soft ∈ {0,1}`. Una sola
    codificación de amplitud `to_amplitude(y) = [√(1-y), √y]` y una sola pérdida (entropía cruzada blanda).
    `label_smoothing` perturba únicamente el `c_y` de inicialización (nunca el objetivo de la pérdida) —
    generaliza el brazo C de `exp_24` como hiperparámetro del modelo, no como una tercera rama de código.
  - **Kernel RBF únicamente**; el comportamiento tipo-coseno para MRI/texto se obtiene normalizando L2 esos
    bloques de características en preprocesamiento (`Normalizer` como último paso, después de MinMax/PCA),
    no con un kernel coseno — `KDMLayer` eleva al cuadrado los valores del kernel, así que un coseno crudo
    de −1 (anti-alineado) obtiene el mismo peso máximo que uno de +1 (alineado); verificado directamente.
  - **Verificación exhaustiva contra los resultados publicados de `exp_23`/`exp_24`** (script en el
    scratchpad de la sesión, no comiteado): 61 chequeos rápidos (higiene de imports, ida-y-vuelta de
    amplitud, equivalencia de la única vía de probabilidad, identidad blanda-generaliza-a-dura, referencia
    Fuzzy KNN, normalización L2 de MRI/texto, RBF-como-coseno, ida-y-vuelta de transferencia unimodal→
    conjunta, ambas cabezas de confianza) más 4 chequeos lentos de reproducción LOOCV completa (~4.5 min):
    Arm B `exp_23` reproducido **exacto** (Macro-F1 `0.6694214876033058`, matriz 48/15/19/6); Arm A
    reproducido a 16 dígitos (`0.5636363636363637`, matriz 44/11/23/10); Arm C de `exp_24`
    (`label_smoothing=0.2` como hiperparámetro del modelo, no como brazo separado) reproducido a 4
    decimales (`0.5719`, matriz 43/12/22/11); y la identidad estructural de `exp_24` reproducida
    exactamente: deriva de `c_y` en cohorte completa = `0.0` exacto a `ε=0`, `≈0.315` a `ε=0.20`.
- **Next Tasks:** Diseñar `exp_25` (`DESIGN.md` → aprobación → `IMPLEMENTATION.md` → aprobación) para
  evaluar `MemKDM` multimodal (tabular+MRI+texto) contra `exp_17` bajo el mismo harness MCCV/LOOCV; nota:
  `utils/embedding-kit/` está vacío en este checkout, por lo que las representaciones `embedkit_*` de MRI/
  texto de `exp_14`/`exp_15` no están disponibles y acotan qué tan comparable puede ser cualquier resultado
  multimodal con `exp_16`/`exp_17`.

## [2026-08-18] - exp_25: KDM Multimodal Basado en Memoria (`MemKDM`) — H1 refutada
- **Current Project State:** `DESIGN.md`/`IMPLEMENTATION.md` de `exp_25` aprobados y ejecutados
  (`experiments/exp_25/scripts/train.py`, 101.3 min, ejecución completa sin errores). `src/` (la capa
  `MemKDM`/harness extraída la sesión anterior) fue comiteado como `b601b2a` antes de la corrida final, así
  `results/git_commit.txt` referencia el código realmente evaluado. Puerta de reproducción (Step 0): Arm B
  de `exp_23` reproducido exacto vía `run_loocv` (`0.6694214876033058`, diff 0.0) antes de cualquier
  resultado multimodal.
- **Specific Advancements:**
  - **H1 (decisión, primaria) refutada, sin ambigüedad.** `joint_trimodal` (LOOCV Macro-F1 0.5860) no
    supera ni al mejor unimodal (`unimodal_txt`, 0.6081), ni a la fusión tardía sin fuga
    (`late_fusion_optimal`, pesos {tab:0.50, mri:0.25, txt:0.25} seleccionados sobre las validaciones de
    Stage 1, 0.6071), ni a `exp_23` Arm B (0.6694). McNemar `joint_trimodal` vs. `late_fusion_optimal`:
    b=7, c=10, p=0.629 (no significativo, pero c>b consistente con la dirección del punto estimado).
    Combinar las tres modalidades en un solo kernel-producto compartido no ayudó en esta cohorte de N=88,
    y específicamente empeoró frente a la modalidad de texto sola.
  - **Hallazgo notable:** las cinco condiciones de Stage 2 (bimodales, trimodal, y el brazo de confianza)
    seleccionaron `sigma_scale=2.0` — el extremo superior de la rejilla — en la búsqueda de Phase A. Patrón
    consistente, no cinco coincidencias independientes; sugiere que la rejilla de `sigma_scale` pudo haber
    quedado corta, no explorada más allá aquí (fuera de alcance de esta corrida).
  - **H2 (confianza, secundaria) parcialmente soportada, margen modesto.** Mejor fila con supervisión
    blanda en todo momento (`target_informed=true` en las 10 condiciones): `joint_tab_mri` / señal 1D
    `w_max` = 0.4547, +0.038 sobre el comparador correcto igual-a-igual (`entropy_soft` de `exp_23`,
    0.4164). Sin prueba de significancia pre-registrada para H2. El resultado proviene de una condición
    **bimodal**, no de `joint_trimodal` (el modelo del que trataba H1).
  - **H3 (estructural) débilmente consistente, no fuertemente evidenciada.** Spearman(`joint_trimodal`
    `h_epistemic`, `late_fusion_equal` `composite_ici`) = −0.117, p=0.276 (no significativo); con `h_total`
    en vez de `h_epistemic`: ρ=−0.245, p=0.021 (nominalmente significativo, dirección negativa). Consistente
    con "las dos construcciones capturan cosas distintas" pero igualmente consistente con "ambas son
    ruidosas" — sin umbral de significancia pre-registrado en `DESIGN.md` para esta comparación.
  - **Verificado, no solo asumido:** `joint_trimodal` y `confidence_arm` (`label_smoothing=0.10`)
    obtuvieron Macro-F1 y AUROC idénticos pese a `label_smoothing` distinto — confirmado que NO es un bug:
    las probabilidades por paciente difieren genuinamente (diff máx. 0.080, ninguna fila idéntica), solo
    coinciden en orden relativo (mismo AUROC) y en los cruces del umbral 0.50 (mismo Macro-F1).
  - Dos simplificaciones de implementación documentadas en el docstring de `train.py` (no cambian el
    protocolo ni las garantías anti-fuga de `DESIGN.md`): sin caché de features por (split, modalidad,
    representación) — coincide con la convención de `exp_13`-`24`; la búsqueda de pesos de fusión usa un
    reajuste dedicado post-hoc de los 3 ganadores de Stage 1 en vez del stash-por-config de
    `IMPLEMENTATION.md` (evita reconstruir una clave desde los dtypes ya coaccionados de `select_best`).
- **Next Tasks:** Considerar un re-barrido angosto de `sigma_scale` más allá de 2.0 (p. ej. {2.0, 4.0, 8.0})
  sobre `joint_trimodal` y `joint_tab_mri` para descartar que el patrón de borde-de-rejilla sea un óptimo
  real vs. una búsqueda truncada. Si se retoma un modelo de decisión multimodal, `joint_tab_mri` (la
  condición conjunta más fuerte, y la fuente de la ganancia de H2) es un objetivo más prometedor que el
  trimodal para un eventual `exp_26`.

## [2026-08-19] - exp_28: Re-evaluación de la generación KNN-duro (exp_5-8) con `BrentMemKDM` — H1 refutada
- **Current Project State:** `DESIGN.md`/`IMPLEMENTATION.md` de `exp_28` aprobados y ejecutados
  (`experiments/exp_28/scripts/train.py`, 1.9 min, ejecución completa sin errores). `src/methods/
  brent_mem_kdm.py` (búsqueda global de Brent en 1D para `sigma` por modalidad, `x`/`y`/`w` nunca
  entrenados, ajuste único congelado — nunca por fold) se aplicó a `exp_5`-`exp_8` (la generación KNN-duro
  original, aún la mejor Macro-F1 LOOCV publicada del repo, 0.7171 de `exp_8`), no a la generación Fuzzy
  KNN que `exp_23`-`27` habían usado como comparador hasta ahora. Puertas de reproducción (Step 0): las
  32/32 verificaciones de `scripts/verify_brent_mem_kdm.py` (ejecutado por primera vez, incluyendo los 2
  chequeos de 100 splits que `--quick` omite) pasaron; G1 reprodujo el KNN tabular de `exp_5` exacto
  (`0.6333333333333333`, 46/14/20/8); G2 reprodujo el KNN de texto de `exp_7` exacto
  (`0.6987539367383268`, 42/21/13/12). `utils/embedding-kit/` sigue vacío en este checkout, así que los
  números publicados de `exp_6`/`exp_8` (basados en `embedkit_sup`) no son objetivos reproducibles aquí —
  se recomputaron referencias KNN honestas (`knn_mri_pca`, `knn_fusion_equal`) sobre la mejor
  representación de MRI disponible.
- **Specific Advancements:**
  - **H1 (primaria) refutada.** `fusion_equal` (voto blando 1/3-1/3-1/3 de los tres arms `BrentMemKDM`)
    alcanza 0.6085 (arm duro) / 0.6452 (arm blando, `target_informed`), ambos muy por debajo de la meta
    0.7171 de `exp_8`. Tampoco supera con significancia su propia referencia KNN honesta recomputada
    (`knn_fusion_equal`, 0.6613): McNemar `fusion_equal__hard` vs. `knn_fusion_equal` p=0.804;
    `fusion_equal__soft` vs. `knn_fusion_equal` p=1.0. **Ninguna comparación en todo el experimento alcanzó
    significancia** (todas las p>0.3, la mayoría >0.5) — cada diferencia de `BrentMemKDM` frente a su
    referencia KNN es consistente con azar en este tamaño de cohorte.
  - **H2 (por modalidad) no soportada para tab/txt.** `tab__hard` 0.5810 vs. `exp_5` publicado 0.6333
    (−0.0523); `txt__hard` 0.6951 vs. `exp_7` publicado 0.6988 (−0.0037, dentro de ruido pero aun así por
    debajo). `mri__hard` 0.5458 vs. `knn_mri_pca` recomputado 0.5299 (+0.0159, único signo positivo, pero
    McNemar p=0.307 no significativo).
  - **Hallazgo estructural notable, en la dirección opuesta a `exp_25`.** Los `sigma_mult` ganadores de
    `exp_27` fueron `tab=0.5, mri=1.0, txt=0.5` sobre una rejilla `{0.25,0.5,1.0,2.0}`/`{0.5,1.0,2.0}`. La
    búsqueda continua de Brent encontró `mri` (0.226) y `txt` (0.20-0.23) **por debajo del punto más
    pequeño que cualquier rejilla previa exploró** — lo opuesto al hallazgo de `exp_25` de que las cinco
    condiciones de Stage 2 se fueron al extremo *superior* de la rejilla (`sigma_scale=2.0`). Ambos
    hallazgos dicen lo mismo: una rejilla discreta tipo `{0.25, 0.5, 1.0, 2.0}` fue demasiado gruesa y mal
    centrada para este problema, en ambas direcciones según la modalidad — exactamente la motivación que
    da el docstring de `brent_mem_kdm.py` para construir una búsqueda continua.
  - **Condición secundaria más cercana a la meta:** `fusion_optimal_leakfree` (pesos {tab:0.40, mri:0.10,
    txt:0.50} seleccionados sobre las validaciones MCCV de Stage 1, nunca sobre LOOCV) alcanzó 0.7068 —
    la más cercana a 0.7171 de todo el experimento, pero no forma parte de H1 (condición de contexto por
    diseño) y no la alcanza.
  - `joint_trimodal` (kernel-producto único, búsqueda `coordinate`, brazo duro solamente) fue la condición
    más débil de todo el experimento (0.5372) — continúa el patrón de `exp_25`/`26`/`27` de que un solo
    kernel compartido entre las tres modalidades rinde peor que las alternativas unimodales o de fusión
    tardía, ahora también bajo un ancho de banda de ajuste continuo en vez de rejilla discreta.
- **Next Tasks:** El código de este experimento (fix de `has_trainable` en `mem_kdm.py`,
  `brent_mem_kdm.py`, `scripts/verify_brent_mem_kdm.py`, `experiments/exp_25`-`28`) sigue sin comitear —
  `results/git_commit.txt` de `exp_28` referencia `b601b2a`, no el árbol real de trabajo; comitear todo
  junto antes de tratar esta corrida como referencia archival. Si se retoma `BrentMemKDM` contra la
  generación KNN-duro, el próximo paso natural es ampliar `bounds_mult` y la rejilla de representaciones
  (solo 2-3 por modalidad en `exp_28`) para verificar si el patrón de sigma bajo-de-rejilla en mri/txt y el
  casi-0.7171 de `fusion_optimal_leakfree` generalizan, en vez de repetir este mismo diseño.
